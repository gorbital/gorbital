# ADR-0029: Threat model: framework, CLI and ecosystem

**Status:** Accepted (2026-09-14) · **Amended by:** ADR-0036 (audit metadata controls), ADR-0053 (internal security review: corrected rows, new rows 33 to 39, review status)

## Context

gorbital generates authentication, handles secrets, installs third-party recipes, runs on developer machines and publishes packages under a vanity import path. The threat model must cover the framework itself, not only apps built with it. This is an architecture-level model; specialist review and penetration testing are still required before 1.0.

## Assets

Developer machines (SSH keys, cloud and GitHub credentials), application source code, generated authentication code, end-user data in generated apps, the `gorbital.dev` domain and DNS, the GitHub organisation and release pipeline, the module index.

## Trust boundaries

CLI ↔ Go module proxy; recipe → owned code; `gorbital.dev` → `go-import` resolution; localhost dev services; CI workflows; app ↔ identity providers; tenant ↔ tenant; public API ↔ ops API.

## Threats and controls

| # | Threat | Control | Milestone |
|---|---|---|---|
| 1 | `gorbital.dev` domain or DNS hijack redirects imports for new versions | Registrar lock, DNSSEC, hardware-key 2FA on registrar and DNS accounts, CAA records, scheduled CI check of `go-import` content | Before any public release |
| 2 | Template injection through user input | Names validated with `go/token.IsIdentifier`; field types from an allowlist; generated code parsed before writing; names, module path, preset, tenancy and mail provider read back from `gorbital.lock` and `gorbital.yaml` validated the same way before rendering | v0.1 |
| 3 | Recipe writes outside the project | All writes through `os.Root`; path validation; `orb` warns when built with a Go release lacking the `os.Root` fixes | v0.1 |
| 4 | Recipe executes code at install time | Declarative operations only; no exec, shell or download operation | v0.1 |
| 5 | Malicious or compromised recipe injects a backdoor | Diff preview, clean git tree, risky new imports highlighted, trust levels for community modules | v0.1 / after 1.0 |
| 6 | Tampered module download | Go module proxy and checksum database; lock file records versions | v0.1 |
| 27 | Tampered or wrong earlier release used as an upgrade's merge base, smuggling code into "unchanged" files | Earlier templates come only from a git tag or commit of a gorbital checkout that is its own repository outside the app, or from the module proxy with checksum verification on (`GOPRIVATE`, `GONOSUMDB` and `GOINSECURE` patterns matched as Go matches them; only sum.golang.org accepted); rendered as text, never compiled or run; every rebuilt file checked against its hash in `gorbital.lock`; files that don't match are compared 2-way and can only conflict ([ADR-0050](0050-upgrades-and-adding-features.md)) | v0.5 |
| 28 | Upgrade silently overwrites or drops developer edits | Hash-proven base; 3-way merges with conflict markers; migrations never merged; clean git tree required; work happens on branch `orb-upgrade/<version>` and conflicts are never committed | v0.5 |
| 29 | Retention used to erase the audit trail: an operator shortens `audit.retention` to delete evidence | Bounded to at least 30 days; changing it needs a reason and records a setting change in history and the audit log; every deletion records a `retention.purged` event with the row count and cutoff ([ADR-0051](0051-operations-v0-5.md)) | v0.5 |
| 30 | Maintenance mode abused or locking staff out | Changing `maintenance.enabled` needs `ops.settings.write`, 2FA and a reason, and is audited; health checks, sign-in and `/ops` stay open so it can be turned off; `go run ./cmd/api maintenance off` works without `/ops` | v0.5 |
| 32 | Client IP spoofing through forwarded headers to escape per-IP limits or falsify audit addresses; one load balancer's address sharing a limit for every client | `X-Forwarded-For` read only from configured trusted proxies (`APP_TRUSTED_PROXIES`), right to left; all-trusting ranges refused; untrusted peers keep their own address; per-IP limits key IPv6 clients by /64; spans' `client.address` is the resolved client | v1.0 |
| 31 | Operational endpoints or tools leaking secrets or infrastructure detail | `GET /ops/system` reports no connection strings, dependency host names, environment variables or settings values, and fixed error descriptions instead of driver messages; `orb doctor` never prints `.env` values; `/ops/releases` lists instance host names by design (accepted, [ADR-0053](0053-internal-security-review.md)) | v0.5 |
| 7 | Compromised CLI release | Releases built only in CI with OIDC, without build caches, from tag commits on `main`, after approval in the `release` environment; Sigstore signatures and provenance, with verification documented; protected tags (tag ruleset); two-person approval for security-sensitive code (`CODEOWNERS`) | v0.1 |
| 8 | Maintainer account takeover | Required 2FA in the GitHub organisation; `CODEOWNERS`; branch protection | Now |
| 9 | GitHub token theft through the CLI | CLI never stores tokens; delegates to `gh`; this repository's workflows use least-privilege `permissions`, actions pinned by SHA, no `pull_request_target`; generated apps ship no CI yet ([ADR-0011](0011-github-integration.md) not implemented) | v0.1 |
| 10 | Secrets committed or shipped | `.env*` git-ignored; templates generated only from files git tracks or would track, so ignored files never reach `orb` or new apps; `.env` kept at mode 0600; gitleaks in this repository's CI (generated apps ship no CI yet); `.env.example` placeholders only | v0.1 |
| 11 | Localhost services abused from a browser (CSRF, DNS rebinding) | Services bound to 127.0.0.1; dev console APIs (`/_dev/`, development only, refused in production) require a `Host` of exactly `localhost`, `127.0.0.1` or `[::1]` with the app's port (DNS rebinding), a loopback peer and a per-run 256-bit bearer token from `orb dev` compared in constant time; no CORS; token never written to disk ([ADR-0065](0065-local-dev-console-apis.md)) | v0.2 / v1.1 |
| 12 | Account enumeration, credential stuffing, sign-in lockout | Uniform responses and response times; rate limits shared by instances (per client network and looser per address for sign-in, per address across every emailed code, per IP with IPv6 grouped by /64); bounded password hashing queue; email normalisation refusing case-folding look-alikes; breached-password hook | v0.2 |
| 13 | Session theft | Hashed tokens, `__Host-` cookies, rotation, idle and absolute expiry, revocation; password and second-factor checks behind a session limited per user and audited | v0.2 |
| 14 | OAuth attacks (CSRF, code injection, token substitution, replayed notifications) | PKCE, state, nonce, issuer and audience checks; automatic linking only for emails the provider is authoritative for, other links made by the signed-in owner; Apple notifications fresh and processed once; `return_to` origins (`APP_CORS_ORIGINS`) https in production; GitHub (no OpenID Connect): state and PKCE S256, the identity read from GitHub's API with the exchanged token, which is never stored, and never authoritative for an address; a link started by a signed-in user (`POST /v1/auth/github/link`, password required) is bound to the browser by the `__Host-oauth` cookie and to the starting session, which must be active at the callback; the default `return_to` is a configured, origin-checked address (`AUTH_DEFAULT_RETURN_TO`, [ADR-0059](0059-github-sign-in.md)) | v0.3, GitHub v1.1 |
| 15 | TOTP secret disclosure or code replay | Encrypted secrets with key rotation, single-use codes, rate limits; recovery codes with 80 bits, beyond offline guessing of stolen hashes | v0.3 |
| 16 | Passkey phishing or origin confusion | Relying-party ID and origin allowlist validation | v0.3 |
| 17 | Cross-tenant data access | Four isolation layers (ADR-0023); an optional fifth, row-level security (`orb add rls`, row 48, [ADR-0061](0061-row-level-security.md)) | v0.4, row-level security v1.1 |
| 18 | Privilege escalation to ops | Platform roles separate from org roles; `/ops/*` requires 2FA; optional internal port | v0.5 |
| 19 | Sensitive data in logs, audit or ops responses | Log IDs not emails; `config.Secret` redaction; audit metadata denylist redaction (sensitive key names in any case style, singular or plural, at any depth); email addresses redacted from mail provider errors, job runs and job logs | v0.2 |
| 20 | SSRF through outgoing requests (webhooks, avatar fetch) | Safe HTTP client blocking private, link-local and metadata addresses | When the feature ships |
| 21 | Vulnerable dependencies | `govulncheck` in repository CI and module checks (generated apps ship no CI yet); CI and releases use the latest Go patch release | v0.1 |
| 22 | Open-core dependency changes (for example River Pro, Atlas Pro) | Dependencies behind modules; licences recorded; swap path documented | Ongoing |
| 23 | Runtime settings abused to weaken security (for example very long session or code expiry) or to leak secrets | No secret type in settings; bounds declared per setting plus hard limits inside library modules; `ops.settings.write` permission (2FA from v0.3); history and audit event (with client IP and user agent) per change; reason required for every security-relevant setting (lifetimes, limits, retention, maintenance, email sender, invitation links) | v0.2 |
| 25 | Invitation takeover: a forwarded, leaked or guessed invitation link used by someone else, or a member inviting with a higher role | Token of 256 bits stored as a hash, single use, expiring; accepting needs a signed-in account whose verified email is the invited one; nobody invites above their own role; an invitation is accepted only while its inviter may still give its role | v0.4 |
| 26 | Organisation enumeration and probing through IDs or error codes | 128-bit random organisation IDs; the same 404 `org_not_found` for missing, deleted and other organisations; resources of other organisations return their own 404 | v0.4 |
| 24 | Job controls abused: destructive jobs run on demand, schedules set to overload the database, security jobs (session purge, retention) disabled | `ops.jobs.write` and `ops.jobs.run` permissions (2FA from v0.3); reason required to disable, reschedule or change timeout, attempts or queue, and to pause a queue; run-now limited to one queued or running run and one a minute; retry only for runs waiting to retry, discarded or cancelled, of enabled jobs; minimum 1-minute interval and bounded timeout and attempts; audit event and history per change; job arguments never returned by ops APIs | v0.2 |
| 33 | Clients forging correlation data: trace context that hides requests from tracing, forces sampling or joins another trace; request IDs reused from other requests in logs, audit events and jobs | Each request starts a new trace linked to the incoming one; incoming baggage ignored; request IDs generated; trace context and request IDs kept only from `APP_TRUSTED_CALLERS` | v1.0 |
| 34 | Privilege escalation inside an organisation: giving a role more powerful than one's own, keeping power through invitations after removal or demotion, acting on a role changed meanwhile | A role may be given only if its permissions are a subset of the giver's; only owners manage owners; invitations checked against the inviter at acceptance; the caller's role re-read under the organisation lock; restore needs the same step-up as delete; deleted accounts leave every organisation | v1.0 |
| 35 | Invitation email abuse: spam or phishing sent from the app's domain through organisations and invitations | A verified address to create organisations and to invite; 20 invitations per organisation per hour; a per-user limit across organisations (`orgs.user_invitations_per_hour`); at most `orgs.max_owned` organisations per user | v1.0 |
| 36 | Account takeover through pre-registration or provider linking: registering someone's address before them, or signing in with a provider account for an address the provider doesn't manage | A contested registration keeps no password; proving the address (code, reset, provider link) ends sessions and removes passkeys, authenticator app, recovery codes and identities added before; automatic linking only for provider-authoritative emails; accounts a provider creates for other addresses stay unverified until proven by email | v1.0 |
| 37 | Unverified accounts used as a foothold: holding an address, a role or an organisation before the owner verifies | Unverified accounts can't sign in with a password or passkey (an account a provider created signs in only with that identity, which proving the address by email removes); deleted after `auth.unverified_account_ttl` (7 days); roles granted only to verified accounts; organisations and invitations need a verified address | v1.0 |
| 38 | Insecure production configuration: a deployment running with development defaults, public API documentation aiding reconnaissance, plain-http origins trusted | `APP_ENV` required at start; `/docs` and the OpenAPI document switched together and off by default in production; `APP_CORS_ORIGINS` https-only in production and parsed strictly everywhere | v1.0 |
| 39 | Denial of service through observability, health and ops endpoints: metric cardinality from `Host` headers, readiness floods draining the database pool, slow audit queries, unbounded hashing or test emails | `Host`-derived attributes dropped from metrics; readiness checks shared and reused for 1 s; audit listing and stats stop after 5 s; password hashing waits at most 5 s for a slot; 5 test emails an hour per operator | v1.0 |
| 40 | Organisation admins weakening security through their organisation's settings, or reading or changing another organisation's | Only settings declared `OrgOverridable` accept organisation values, within the platform's bounds, validation and reason requirement; the apps' `TestSecuritySettingsArentOrgOverridable` fails for sign-in, rate limit, retention, maintenance, mail and link-target settings; every organisation settings route starts with `orgs.RequireMember` (404 `org_not_found` to non-members); changes need `orgs.settings.write` (owner, admin), are versioned, kept in history and audited with the organisation; values are purged with the organisation ([ADR-0056](0056-per-organisation-settings.md)) | v1.1 |
| 41 | Idempotency keys replaying one caller's response to another, running a request twice under retries or races, or keeping secrets and personal data in stored responses | Keys scoped to the authenticated actor (kind and ID) and hashed with it; anonymous callers ignored; one-statement atomic claim with a lock token; fingerprint of method, target and body; `/v1/auth/*`, `Set-Cookie`, `Cache-Control: no-store` (new API keys) and `DontStore` responses never stored; bounded retention (1 h to 7 days) and hourly cleanup; the table isn't exposed through `/ops` ([ADR-0060](0060-idempotency-keys.md)) | v1.1 |
| 42 | Forged or replayed provider webhooks suppressing a user's address (denying them password resets and codes) or flooding the suppression list; the list leaking addresses | Resend's Svix HMAC-SHA256 signature over ID, timestamp and raw body, every signature compared in constant time; 5-minute timestamp tolerance; applied `svix-id`s stored in the same transaction as the suppressions, so a replay changes nothing; endpoint 404 without `RESEND_WEBHOOK_SECRET`; 256 KiB body limit; malformed secret refused at start without being printed. Addresses normalised, never in logs or audit events, visible only with `ops.mail.read`; removal needs `ops.mail.write` (2FA roles) and a reason, and is audited ([ADR-0062](0062-resend-webhooks-and-suppression-list.md)) | v1.1 |
| 43 | Metrics endpoint exposing service internals (routes, load, error rates, runtime) to the internet, or flooded with client-chosen label values to hide real series | Off by default; a separate listener serving only `GET /metrics`; `METRICS_ADDR` on the API's port refused at start; private binding and per-instance scraping documented; attributes limited to route patterns, normalised methods and status codes (`Host`, paths, queries, user agents, forwarded addresses and trace headers never become labels); scrape concurrency and time bounded ([ADR-0063](0063-prometheus-metrics.md)) | v1.1 |
| 44 | Feature flags exposing unreleased features or targeting, being changed without trace, diverging between instances, or used as access control | Only flags declared `Client()` are listed to clients, and only their answers (key → bool) for the caller, `Cache-Control: private, no-store`; `GET /v1/flags` needs `flags.flag.read` (the `user` role, so API keys need it in their scopes); organisation flags only after `RequireMember` (404 `org_not_found` otherwise); changes need `ops.flags.write` (2FA-required `platform_admin`), a version and a reason, with history and audit events (IDs kept out of audit metadata); bounded lists (1,000 IDs, visible ASCII ≤ 100 bytes) and percentage 0–100; `NOTIFY` plus 5-minute resync; guide and ADR state flags aren't access control ([ADR-0057](0057-feature-flags.md)) | v1.1 |
| 45 | API key leakage: keys in logs, audit events, caches, stored idempotent responses, databases or source code, or a leaked key made permanent | Shown once with `Cache-Control: no-store`, which the idempotency middleware never stores; only SHA-256 stored; lookup ID (not the secret) in logs and audit events; `gbk_` prefix for secret scanners; required expiry (at most a year); keys can't create keys, change the account, or join or leave organisations (`session_required`); revoked by the owner, by disabling a service account, account deletion, password reset and address claims ([ADR-0058](0058-api-keys-and-service-accounts.md)) | v1.1 |
| 46 | Privilege escalation through API keys: scopes beyond the owner, 2FA-required roles, a service account acting in another organisation, operations no scope limits, keys guessed | Permissions read from the owner's current roles on every request and intersected with scopes (`Principal.Restrict`, applied by the app, `auth.WithPrincipal` and `orgs.Authorize`); every signed-in operation checks a permission, what any user may do granted by the `user` role every user holds, so scopes bound it; joining or leaving organisations needs a session; roles in `RequireMFA` never grant keys and can't be given to service accounts; organisation service accounts are refused outside their organisation and hold only roles their manager could give; 256-bit secrets compared in constant time; failed keys rate-limited per client network ([ADR-0058](0058-api-keys-and-service-accounts.md)) | v1.1 |
| 47 | Live observability and incidents: request data exposing clients' paths or identities, clients growing series without bound, SSE streams outliving the session or exhausting instances, forged or duplicated automatic incidents, incident reports leaking audit details or injecting markup | Series keyed only by method (standard or `_OTHER`) and route pattern from code; paths, queries, headers, addresses and IDs never stored; 500 series a minute and 11 minutes in memory per instance; `/ops/observability/*` behind `ops.observability.read` with 2FA roles; streams checked on connect and before every event (session re-authenticated, permission rechecked), 10-minute limit, 2 per user and 20 per instance, closed on shutdown, per-write deadlines; incidents need `ops.incidents.write` and are audited without their text; detection under an advisory lock with a unique index for one open automatic incident; reports read-permission-gated per section, audit events without IP, user agent, labels or metadata, Markdown escaped ([ADR-0064](0064-live-observability-and-incidents.md)) | v1.1 |
| 48 | Cross-organisation read or write through a query missing its `org_id` filter (multi-tenant apps) | Four layers (`orgs.RequireMember`, repositories taking `orgs.ID`, composite keys, generated cross-organisation tests); optional fifth layer `orb add rls`: forced `org_isolation` policies on every `org_id NOT NULL` table (`org_members` and `org_invitations` left out by design) reading the organisation every connection carries from its context; bypass only in code with a reason (`postgres.WithoutRowLevelSecurity`), logged and named on spans, never from request input; startup, `migrate --status` and `orb doctor` warn about superuser or `BYPASSRLS` roles, unforced tables and organisation tables without a policy ([ADR-0061](0061-row-level-security.md)) | v1.1 |
| 49 | Dev console APIs leaking secrets or request data, or left on outside a developer's machine | Off unless `APP_ENV=development` and `DEV_CONSOLE_TOKEN` are set; the app refuses to start with the token in production; `/_dev/config` shows secrets and secret-looking names only as set or unset and redacts URL user info, queries and fragments; recorded requests keep paths but no query strings, headers, cookies or bodies; logs and requests in bounded ring buffers, streams limited and ended on shutdown; `Cache-Control: no-store`; not in the app's OpenAPI document; the checks of row 11 ([ADR-0065](0065-local-dev-console-apis.md)) | v1.1 |

## Residual risk

Owner: project maintainer. Each accepted risk is recorded here with a review date. An external security review of authentication, sessions, tenancy and the generator is required before 1.0; an internal review came first ([ADR-0053](0053-internal-security-review.md), [report](../security/2026-09-internal-review.md)).

| Accepted (2026-09-16) | Threat | Reason | Review |
|---|---|---|---|
| `/ops/releases` lists instance host names | 31 | Operator-only with 2FA; identifies instances during rollouts | Before 2.0, or when `/ops` gains a less trusted role |
| Run-now, retry, cancel, pause and resume have no history table; a failed audit write leaves only a log line | 29 | Transactional audit writes would let an audit outage block incident response | With the external review |
| `/version` shows the version, Go version and commit | 38 | Public by design for release tracking; can be blocked at the load balancer | With the external review |
| `go 1.26.0` directive kept, below the release with every `os.Root` fix | 3 | ADR-0015 sets the minimum Go version; `orb` warns below 1.26.5, CI and releases use the latest patch | Each Go minor release |
| Response-time floor can be exceeded by a slow database, showing timing differences again | 12 | The floor covers normal load; per-address limits bound probing | With the external review |
| No per-recipient invitation limit | 35 | Per-user limits on verified accounts bound each attacker account | When invitation volume is observed in production |
| A request running longer than the 5-minute idempotency lock can run twice; stored responses keep personal data up to `idempotency.retention` | 41 | Longer locks would block legitimate retries after a crash; retention is bounded to 7 days and cleaned hourly | When long-running POST endpoints appear |
| Anyone holding `RESEND_WEBHOOK_SECRET` can suppress any address; `/ops` has no lookup by address | 42 | The secret is treated like the provider API key; operators remove suppressions with a reason; no addresses in URLs | With the external review |
| The metrics listener has no authentication | 43 | Network isolation is the control (off by default, refused on the API port, private binding documented); `METRICS_TOKEN` deferred | When a deployment needs scraping across a network boundary |
| User and organisation IDs in flag lists and history outlive deleted accounts and organisations until edited and until history retention; a client learns which client flags are on for it | 44 | IDs aren't personal data on their own; history retention (`ops.history_retention`) bounds it; client flags are declared as client-visible | When flags gain non-ID targeting |
| `/ops` can't be automated with API keys; personal keys act in every organisation of the user and survive a password change made while signed in; many clients behind one NAT share the failed-key limit | 45, 46 | Keeping `/ops` human-only with 2FA outweighs automation; per-organisation personal keys and revocation on password change are later options; valid keys aren't limited | With the external review |
| GitHub access tokens aren't revoked after the two API reads; linking GitHub from a frontend on another site fails in browsers blocking third-party cookies | 14 | The token is never stored and lives only for the callback; the link fails closed | When GitHub revocation or cross-site linking is requested |
| Incident titles and messages are free text kept without retention; a stream sees a revoked session up to 5 s late; `ops_viewer` sees route patterns and instance IDs | 47 | Incidents are the operational record (docs ask for no personal data); the recheck runs before every event; route patterns come from code | With the external review |
| Row-level security doesn't apply to superuser or `BYPASSRLS` roles (including the local Docker database's default user); the bypass setting can be changed by SQL running as the app's role; session settings don't survive a pooler in transaction mode; `org_members` and `org_invitations` keep four layers | 17, 48 | The app warns at startup and `orb doctor` reports bypassing roles; the layer defends against a forgotten filter, not SQL injection (queries use placeholders); a separate `BYPASSRLS` system role is a later option; poolers in session mode work; the two tables are read before an organisation is known | With the external review, or when a second pool with a system role is added |
| Other processes of the same OS user can read the dev console token from the app's environment; the console shows non-secret configuration values, recent request paths (without queries) and log records to anyone holding the token | 11, 49 | The console assumes the developer's user account isn't compromised; the token lives only for one `orb dev` run and never on disk; it's development only | When the console serves more than one local user |

## Consequences

- `SECURITY.md` process in place before the repository is public.
- Every milestone's definition of done includes its rows from this table.

## v0.1 status (2026-09-14)

| # | Status |
|---|---|
| 1 Domain/DNS hijack | **Open, maintainer action** before any public release: registrar lock, DNSSEC, hardware-key 2FA, CAA records, `go-import` monitoring |
| 2 Template injection | Done: name and module path validation, templates rendered with fixed data, Go output parsed by `go/format` |
| 3 Writes outside project | Done: `os.Root` for app creation and template generation |
| 4 Code execution at install | Done: `orb new` runs only `go mod tidy` and `git init` |
| 5 Malicious recipes | Not yet applicable: no third-party recipes; diff preview arrives with `orb add` |
| 6 Tampered downloads | Done: Go module proxy and checksum database; lock file records hashes |
| 7 Compromised CLI release | Ready: release workflow with keyless Sigstore signing and build provenance; takes effect on the first `cli/v*` tag. *Corrected 2026-09-16 (CLI-8):* protected tags and two-person approval are repository settings (tag ruleset, `release` environment reviewers, `CODEOWNERS` teams) and can't be verified from the repository |
| 8 Maintainer takeover | **Open, maintainer action:** require 2FA in the `gorbital` organisation, branch protection, `CODEOWNERS` |
| 9 GitHub token theft | Done for this repository's workflows: CLI stores no tokens; workflows use read-only permissions, SHA-pinned actions, no persisted credentials, no `pull_request_target`. *Corrected 2026-09-16 (CLI-8):* generated apps ship no CI ([ADR-0011](0011-github-integration.md) not implemented) |
| 10 Secrets committed | Partly done: `.env*` ignored in generated apps; gitleaks only in this repository's CI. *Corrected 2026-09-16 (CLI-8):* generated apps ship no CI |
| 19 Sensitive data in logs | Done for v0.1 scope: `config.Secret` redaction, access logs without query strings, validation errors never echo values |
| 21 Vulnerable dependencies | Partly done: govulncheck in this repository's CI for every module; generated apps have no CI yet (CLI-8, 2026-09-16) |

## v0.2 status (2026-09-15)

| # | Status |
|---|---|
| 11 Localhost services | Done for PostgreSQL, Mailpit and Grafana: `compose.yaml` binds them to 127.0.0.1 in the repository and both presets |
| 12 Account enumeration | Done (ADR-0038): identical responses for register, resend and forgot-password; `invalid_credentials` for unknown addresses and wrong passwords after the same hashing work; 10 login attempts per address per 15 minutes; 5 attempts per code; 60 auth requests per IP per minute; breached-password hook (`PasswordChecker`). Limits shared across instances from v1.0 (see below) |
| 13 Session theft | Done (ADR-0038): 256-bit tokens stored as SHA-256; `__Host-` Secure HttpOnly SameSite=Lax cookies with cross-origin protection; idle (14 d) and absolute (90 d) expiry clamped to hard limits; revocation, logout-all, sessions ended on password change, reset and deletion |
| 18 Privilege escalation to ops | **Partial:** `/ops/*` requires a session whose platform roles grant each operation's permission (deny by default, roles read on every request, grants audited); `OPS_TOKEN` removed (ADR-0038). Required 2FA for ops roles and the optional internal port remain |
| 19 Sensitive data in logs | Done for new modules: database URLs never appear in errors, query spans record SQL text but not arguments, validation errors never echo values, job arguments never appear in ops responses, ops token never printed. Audit metadata (`modules/auditpg`, ADR-0036): values under sensitive keys (`password`, `secret`, `token`, `otp`, `recovery_code`, …) redacted at any depth, size bounded, text sanitised; audit events append-only through a trigger. A per-action metadata allowlist was considered and replaced by recorder-side redaction. Email (ADR-0037): the Resend API key and SMTP password are `config.Secret`s from the environment, never flags, runtime settings or error text; `orb add mail` saves typed secrets only to a git-ignored `.env` (mode 0600) and never prints them; SMTP credentials are sent only over TLS or to a local server; test email audit events omit the recipient; development email goes to Mailpit, never real people |
| 22 Open-core dependencies | River (MPL-2.0) and goose (MIT) used without Pro features; `robfig/cron/v3` (MIT) swap path is River's `PeriodicSchedule` |
| 23 Runtime settings abuse | Done: no secret type, declared bounds, reasons, versions, history and audit events (`modules/settings`) |
| 24 Job controls abuse | Done: permissions per operation, reasons to disable or reschedule, 1-minute minimum interval, bounded timeout and attempts, history and audit events, arguments hidden (`modules/jobs`) |

Reviewed against the code for v0.2's definition of done (rows 12, 13, 19, 23 and 24), 2026-09-15: each mitigation above is in place and tested. Notes from the review:

- Row 12: the breached-password hook is `authusecase.Config.PasswordChecker`. No checker is configured by default; an app chooses one (a local list, or a k-anonymity API) in `internal/app/module_auth.go`.
- Row 19: the auth use cases' log line for an email that couldn't be queued named its attribute `email`, which reads like an address; the value was always the email's kind (`verification_code`), never the address. It is now `email_kind`.
- Row 19: seed data (ADR-0042) prints the administrator's random password once to the developer's terminal, never to logs, files or the audit log, and refuses to run in production.
- Row 24: disabling requires a reason, a disabled job can't be run now (409) and leaves the schedule, and each change is in the job's history and audit log (`TestJobsThroughOps`).

## v0.3 status (in progress, 2026-09-15)

| # | Status |
|---|---|
| 15 TOTP secret disclosure or code replay | Done ([ADR-0043](0043-two-factor-authentication.md)): secrets encrypted with AES-256-GCM under `AUTH_ENCRYPTION_KEYS`, with key IDs, bound to the user ID and re-encrypted by `rotate-auth-keys`; a code is accepted only for a later time step than the last one used, checked and recorded in one statement (safe across instances); sign-in challenges allow 5 attempts in 5 minutes and count toward the per-address login limit; recovery codes are hashed and single-use; secrets, codes and recovery codes never appear in logs or audit metadata |
| 18 Privilege escalation to ops | Required 2FA done ([ADR-0043](0043-two-factor-authentication.md)): `platform_admin` and `ops_viewer` grant their permissions only to sessions verified with a second factor, in every environment; the rule is code, not a runtime setting; other roles' permissions are unaffected. The optional internal port remains (v0.5) |
| 16 Passkey phishing or origin confusion | Done ([ADR-0044](0044-passkeys.md)): the relying party ID and allowed origins come from the environment (not runtime settings), each origin must be https (http only for localhost in development) on the RP ID or a subdomain; every response's origin, RP ID hash, challenge, signature and user-verification flag are checked by `modules/auth/passkey`; ceremonies are single use and bound to their purpose, user and sign-in challenge; counters that don't increase are refused and audited; Android apps are allowed only by configured certificate fingerprints |
| 14 OAuth attacks | Done ([ADR-0046](0046-google-and-apple-sign-in.md)): web sign-ins keep a single-use, 10-minute state on the server bound to an HttpOnly `__Host-oauth` cookie (login CSRF), a nonce and a PKCE S256 verifier (Google); ID tokens are checked by `modules/auth/social` for signature against the provider's keys, issuer, an audience among the configured client IDs, expiry, an age under 10 minutes and the nonce; native apps must use a server-issued single-use nonce (hashed for Apple); `return_to` is limited to the API's and `APP_CORS_ORIGINS` origins and results go in the URL fragment; linking requires a provider-verified email and removes the password and sessions of an account that never verified its address (pre-account hijacking); the second factor still applies; Apple tokens are revoked on deletion and Apple's signed notifications are verified |

## v0.4 status (done, 2026-09-15)

| # | Status |
|---|---|
| 17 Cross-tenant data access | Done in `examples/full-multi` and multi-tenant generated apps ([ADR-0048](0048-organisations-v0-4.md)): every org use case starts with `orgs.RequireMember`, which reads the membership on each call; org-scoped repository methods take the organisation ID in every query; org-scoped tables have `org_id NOT NULL` with a cascading foreign key and `UNIQUE (org_id, id)` for composite references; cross-organisation denial tests are generated for every org-scoped resource (`orb gen resource --scope org`); a drift test keeps `full-multi` identical to `full-single` outside the files organisations change. Row-level security remains the v1.1 option |
| 25 Invitation takeover | Done: 256-bit tokens stored as SHA-256 in the URL fragment of the link, single use, replaced on resend, revocable, expiring (`orgs.invitation_ttl`, 1 to 30 days); accepting requires a signed-in account whose verified email equals the invited address; nobody invites, promotes or removes above their own role and only owners manage owners; 20 invitations per organisation per hour; every step is audited without email addresses in metadata |
| 26 Organisation enumeration | Done: organisation IDs carry 128 random bits; non-members, deleted and missing organisations all get 404 `org_not_found`, checked before the resource is read; a resource ID from another organisation returns that resource's 404 |
| 18 Privilege escalation to ops | Organisations keep their own role catalog: `RequireMember` replaces the actor's platform permissions with the member's org-role permissions for org operations, org roles never grant `/ops` permissions, and platform roles grant no access to organisations' data |

## v0.5 status (done, 2026-09-15)

| # | Status |
|---|---|
| 27 Tampered earlier release | Done in `orb upgrade` ([ADR-0050](0050-upgrades-and-adding-features.md)): `git archive` of a validated tag or commit (never an option or range) extracts only regular files under the templates through `os.Root`; `go mod download` is refused when checksum verification is off for `gorbital.dev/cli`; every rebuilt file is checked against `gorbital.lock`, a v1 lock must match completely, and a v2 lock with no matching file stops the upgrade. Tests: `TestExtractTemplatesStaysInside`, `TestReleaseFromCheckoutRejectsRefs`, `TestChecksumPolicy`, `TestUpgradeFromV1Lock` |
| 28 Upgrade drops edits | Done: `cli/internal/merge` keeps every line the developer added (`TestPlan`); unproven bases can't take the template silently; dirty trees are refused, conflicts exit 1 uncommitted on the branch; an app edited with `orb` at `v0.4.0` upgraded with every edit kept (the end-to-end upgrade test, now `TestUpgradeFromRelease`, renamed after the gorbital rename and run from releases after it, [ADR-0050](0050-upgrades-and-adding-features.md)) |
| 17 Cross-tenant data access after `orb add orgs` | Done: converted data lands only in its owner's personal workspace, which only that owner belongs to; converted organisation IDs carry 128 random bits like `orgs.NewID`'s; the converted schema has the same `org_id NOT NULL`, cascading foreign key and `UNIQUE (org_id, id)` as a new multi-tenant app, and the converted app passes the multi-tenant suite including the cross-organisation denial tests (`TestAddOrgsConvertsADatabase`). Resources the developer generated stay user-scoped, as they were |
| 18 Privilege escalation to ops | Every ops use case calls `authorize` before touching a dependency, checked for every exported method without a session, without permissions and needing 2FA (`TestEveryOperationAuthorizesFirst`); every `/ops` operation declares bearer security and 401 and 403 (`TestOpsOperationsDeclareSecurity`) |
| 29 Erasing the audit trail | Done: `audit.retention` bounded to 30 days–10 years with a required reason; `retention.purged` recorded per deletion (`TestWorkerDeletesInBatchesAndRecords`) |
| 30 Maintenance mode abuse or lockout | Done: reason required, open routes and break-glass command (`TestMaintenanceMode`, `TestMaintenanceCommand`) |
| 31 Leaks from ops endpoints and tools | Done: `TestOpsSystem` checks the report holds no connection string; `TestDoctorFindsProblems` checks `orb doctor` never prints a `.env` secret |

## v1.0 status (built, awaiting external review)

| # | Status |
|---|---|
| 12 Credential stuffing across instances | Done ([ADR-0052](0052-shared-rate-limits.md)): sign-in, per-IP and two-factor limits are decided in PostgreSQL by `modules/ratelimitpg`, so N instances allow each limit once, not N times (`TestSharedAcrossInstances`, `TestSignInLimitSharedAcrossInstances`); keys are hashed; a database outage falls back to per-instance limits rather than none; a local pre-check refuses floods without database writes (`TestLocalPreCheck`); the limits are bounded runtime settings requiring a reason |
| 32 Client IP spoofing | Done: `httpx.TrustedProxies` uses `X-Forwarded-For` only from `APP_TRUSTED_PROXIES` peers and refuses all-trusting ranges (`TestTrustedProxies`, `TestParseTrustedProxies`); an untrusted peer claiming new addresses is still limited as itself (`TestPerIPLimitBehindTrustedProxy`) |
| 33 Forged correlation data | Done ([ADR-0007](0007-observability.md), [ADR-0030](0030-context-and-correlation.md)): `telemetry.HTTPMiddleware` starts a new trace per request linked to the incoming span context and ignores baggage unless the caller is in `APP_TRUSTED_CALLERS` (`TestHTTPUntrustedTraceContext`, `TestHTTPTrustedTraceContextKeepsSampling`); `httpx.RequestIDFrom` generates request IDs for other callers (`TestRequestID`, `TestRequestIDFromTrustedCallers`) |
| 34 Privilege escalation inside an organisation | Done ([ADR-0048](0048-organisations-v0-4.md)): `TestCustomRolesCantExceedTheAssigner`, `TestInvitationsEndWithTheInvitersRole`, `TestRoleIsCheckedAgainUnderTheLock`, `TestRestoreNeedsTheSameStepUpAsDelete`, `TestDeletedAccountsDontStayOwners` |
| 35 Invitation email abuse | Done: `TestOrganisationAndInvitationLimits`, `TestOwnedOrganisationLimitHoldsUnderConcurrency`; no per-recipient limit (accepted, see residual risk) |
| 36 Pre-registration and linking takeover | Done ([ADR-0038](0038-authentication-v0-2.md), [ADR-0046](0046-google-and-apple-sign-in.md)): `TestPreRegistrationTakeover`, `TestRegisterAgainKeepsOnlyTheSamePassword`, `TestVerifyingRemovesWhatCameBefore`, `TestSocialLinksOnlyAuthoritativeEmails`, `TestSocialLinkingEndToEnd`, `TestNonAuthoritativeSocialAccountIsClaimedByEmail`. Accounts verified before 2026-09-16 with a password set by an earlier registrant can't be told apart; the upgrade notes give a query |
| 37 Unverified accounts | Done: `TestCleanupExpiresUnverifiedAccounts`, `TestRolesGrantPermissions`, `TestOrganisationAndInvitationLimits` |
| 38 Insecure production configuration | Done ([ADR-0020](0020-constructors-and-configuration.md), [ADR-0027](0027-api-contract-and-docs.md), [ADR-0052](0052-shared-rate-limits.md)): `TestLoadConfigSecureDefaults`, `TestDocs`, `TestWithoutSpecEndpoints`, `TestCORS`, `TestDevSetsAppEnv`; `/version` detail accepted |
| 39 Denial of service through observability, health and ops endpoints | Done: `TestHTTPMetricsIgnoreHost`, `TestReadinessSharesChecks`, `TestListAndStatsTimeOut`, `TestHasherWaitIsBounded`, `TestSendTestEmailNeedsOnlyItsPermissionAndIsLimited` |

## Internal security review (2026-09-16)

[ADR-0053](0053-internal-security-review.md): six area reviews, 43 distinct findings, every one fixed with a regression test or accepted in writing ([report](../security/2026-09-internal-review.md)). Rows the review changed, besides the new rows 33 to 39 above:

| # | Status after the review |
|---|---|
| 2 Template injection | Done: lock and manifest inputs validated before rendering (CLI-4; `TestReadLockRejects`, `TestReadManifestRejectsInvalidInputs`) |
| 3 Writes outside project | Done: `orb version` and `orb doctor` warn when `orb` was built with Go older than 1.26.5 (CLI-7; `TestToolchainWarning`); the `go 1.26.0` directive is accepted |
| 7 Compromised CLI release | Ready: release job needs approval in the `release` environment, builds only commits on `main`, without caches, and runs the CLI tests first; `CODEOWNERS` covers release-critical files; signature and provenance verification in the CLI guide (CLI-6). **Open, maintainer action:** create the `release` environment with required reviewers and the tag ruleset; not run on GitHub Actions yet |
| 9 GitHub token theft | Done for this repository; generated apps ship no CI (corrected, CLI-8) |
| 10 Secrets committed or shipped | Partly done: templates built from git's file set (CLI-1; `TestSkippedLocalEnvironmentFiles`, `TestRunLeavesGitIgnoredFilesOut`); `orb add mail` makes `.env` 0600 (CLI-2; `TestAddMailMakesAnExistingEnvPrivate`); gitleaks only in this repository's CI |
| 12 Account enumeration, credential stuffing, lockout | Done: code budget across codes (AUTH-S-2; `TestCodeChecksAreLimitedAcrossCodes`), look-alike addresses refused (AUTH-S-3; `TestTokensCodesAndEmails`), response-time floor (AUTH-S-4; `TestAnonymousFlowsTakeTheSameTime`), per-network and per-address sign-in limits (AUTH-S-6; `TestLoginLimitIsPerNetwork`), IPv6 /64 keys (AUTH-S-7; `TestPerIPLimitGroupsIPv6`), bounded hashing and reset codes checked before hashing (AUTH-S-8; `TestHasherWaitIsBounded`, `TestResetChecksTheCodeBeforeHashing`) |
| 13 Session theft | Done: per-user limit on password and second-factor checks behind a session, audited as `auth.reauth.failed` (AUTH-S-5, AUTH-M-2; `TestChecksBehindASessionAreLimited`) |
| 14 OAuth attacks | Hardened: authoritative-email linking and `POST /v1/auth/identities` (AUTH-M-1; `TestAuthoritativeEmail`, `TestSocialLinkingEndToEnd`), Apple notification freshness and replay (AUTH-M-3; `TestAppleNotificationFreshness`, `TestAppleNotificationReplayEndToEnd`), https `APP_CORS_ORIGINS` in production (HTTP-4; `TestLoadConfigSecureDefaults`) |
| 15 TOTP and recovery codes | Done: 80-bit recovery codes; existing codes still verify (AUTH-M-4; `TestRecoveryCodes`) |
| 17 Cross-tenant data access | Reviewed: no cross-organisation read or write found; every org-scoped query filters on `org_id` from the authorised path, including resource templates |
| 18 Privilege escalation to ops | Reviewed: every `/ops` operation authorises first; `POST /ops/mail/test` now authorises fully before queueing (OPS-8; `TestSendTestEmailNeedsOnlyItsPermissionAndIsLimited`) |
| 19 Sensitive data | Done: redaction by normalised key segments (OPS-4; `TestRecordRedactsAndSanitizes`); email addresses redacted from provider errors, job runs and logs (OPS-5; `TestRedactAddresses`, `TestFailedAttemptsAreLoggedOnce`). The row said "allowlist"; the code has always used a denylist, now stated |
| 21 Vulnerable dependencies | Partly done: grpc v1.83.2 in the Minimal preset and `modules/telemetry` (CLI-7); govulncheck only in this repository's CI |
| 23 Runtime settings abuse | Done: reasons required for `mail.from_name`, `mail.from_email`, `mail.reply_to`, `auth.verification_code_ttl`, `orgs.invitation_url`, `orgs.invitation_ttl` (OPS-9; `TestEmailThroughOps`); audit events carry the client IP and user agent (OPS-7; `TestRuntimeSettingsThroughOps`); `StringList` settings capped at 100 items by default (OPS-10) |
| 24 Job controls abuse | Done: reasons for timeout, attempts and queue changes and queue pauses, run-now limited (OPS-2; `TestUpdateRequiresReasonForLimits`, `TestRunNowIsLimited`), retry refused for completed runs and disabled jobs (OPS-3; `TestRetryRefusesCompletedJobsAndDisabledDefinitions`) |
| 25 Invitation takeover | Done: inviter re-checked at acceptance (ORG-1; `TestInvitationsEndWithTheInvitersRole`) |
| 26 Organisation enumeration | Reviewed: unchanged; role checks now re-read under the organisation lock (ORG-4) |
| 27 Tampered earlier release | Done: trailing-slash patterns and other checksum databases refused (CLI-3; `TestMatchesModulePrefix`, `TestChecksumPolicy`); checkouts inside the app refused (CLI-5; `TestReleaseCheckoutRefusesCheckoutInsideApp`, `TestReleaseFromCheckoutNeedsAGorbitalCommit`) |
| 28 Upgrade drops edits | Done: the end-to-end test is `TestUpgradeFromRelease`; it skips until a release after the gorbital rename is tagged, and passes from the rename commit |
| 29 Erasing the audit trail | Unchanged; audit-write durability for run-now, retry, cancel, pause and resume accepted (OPS-10) |
| 31 Leaks from ops endpoints | Unchanged; instance host names in `/ops/releases` accepted (OPS-10) |
| 32 Client IP spoofing | Done: IPv6 keyed by /64 (`TestByRemoteIPGroupsIPv6By64`); spans' `client.address` from the resolved client (HTTP-1; `TestHTTPUntrustedTraceContext`) |

## v1.1 status (done, 2026-09-16)

| # | Status |
|---|---|
| 40 Per-organisation settings | Done ([ADR-0056](0056-per-organisation-settings.md)): `TestSecuritySettingsArentOrgOverridable` in both Full apps; `TestOrganisationsCantReachEachOthersSettings` (non-members get 404 `org_not_found`); members without `orgs.settings.write` get 403 `forbidden` and platform-only settings 404 (`TestOrganisationSettingsEndToEnd`) |
| 41 Idempotency keys | Done ([ADR-0060](0060-idempotency-keys.md)): `TestCallersDontShareKeys` (two users and a service account with the same key get their own responses; no raw key or ID stored), `TestRacingRequestsRunOnce` and `TestConcurrentRequests` (one run, others 409 or replay), `TestFingerprintMismatch` (422), `TestReleasedResponses` (5xx, panic, 401/403/429, `Set-Cookie`, body cap, `DontStore`), `TestStaleLockExpires`; apps: `TestIdempotencyKeys` (sign-in never replayed) and `TestIdempotencyKeysStayInTheirOrganisation` (full-multi). Residual risk accepted above |
| 42 Provider webhooks and suppression list | Done ([ADR-0062](0062-resend-webhooks-and-suppression-list.md)): `TestVerifyWebhook` (Svix's published vector and a Resend vector; expired, future, altered body, other ID, other secret, other versions and missing headers refused), `TestAddOnce` (replays refused, failed deliveries retryable, 10 concurrent deliveries apply once); apps: `TestResendWebhook` (end to end with signatures; replay after removal doesn't re-suppress; audit without addresses), `TestSuppressedAddressesGetNoEmail`, `TestWebhookNeedsItsSecret`. Residual risk accepted above |
| 43 Metrics endpoint exposure and label flooding | Done ([ADR-0063](0063-prometheus-metrics.md)): `TestLoadConfigMetricsAddr` (off unless set, refused on the API's port), `TestMetricsListener` (only `/metrics` on the listener, not on the API), `TestPrometheusIgnoresClientValues` (200 requests with client-chosen methods, paths, `Host`, forwarded addresses, user agents, trace headers and baggage give two series and none of those values); at most 4 concurrent scrapes of 10 s. No authentication accepted above |
| 23 Runtime settings abuse | Per-organisation values only for settings declared `OrgOverridable`, guarded by `TestSecuritySettingsArentOrgOverridable` ([ADR-0056](0056-per-organisation-settings.md)) |
| 19 Sensitive data | Suppressed addresses never logged or audited; `idempotency_keys` stores hashed scopes and keys, never `/v1/auth/*` responses or `Cache-Control: no-store` responses such as new API keys ([ADR-0060](0060-idempotency-keys.md), [ADR-0062](0062-resend-webhooks-and-suppression-list.md)); API keys logged and audited only by lookup ID (`TestAPIKeysNeverLogged`, [ADR-0058](0058-api-keys-and-service-accounts.md)); observability stores no paths, queries or client values ([ADR-0064](0064-live-observability-and-incidents.md)) |
| 44 Feature flags | Done ([ADR-0057](0057-feature-flags.md)): library `TestRulePrecedence`, `TestRolloutDistribution` (10,000 subjects), `TestBucketIsStable`, `TestAnonymousCallersGetOnlyWholeRollouts`, `TestSetRejectsInvalidChanges`, `TestInstancesConvergeThroughNotifications`, `TestClientFlags`; apps `TestFeatureFlagsEndToEnd` (permissions, reason, bounds, conflicts, audit, only client flags), `TestFlagChangeReachesAnotherInstance`, `TestAPIKeyScopesCoverOwnData` (`GET /v1/flags` refused to a key without `flags.flag.read`), full-multi `TestOrganisationFlagsEndToEnd`, `TestOrganisationsCantReachEachOthersFlags`. Residual risk accepted above |
| 45 API key leakage | Done ([ADR-0058](0058-api-keys-and-service-accounts.md)): `TestNewAPIKey`, `TestAPIKeyMatchesInConstantTime`, `TestNewTokenIsNeverAnAPIKey`, `TestAPIKeysNeverLogged`, `TestAPIKeysNeedASession`, `TestDeletedOrDisabledOwnersKeysFail`, `TestCleanupRecordsExpiredAPIKeys`; apps `TestAPIKeysEndToEnd`; idempotency never stores `Cache-Control: no-store` responses (`TestReleasedResponses`). Residual risk accepted above |
| 46 Privilege escalation through API keys | Done ([ADR-0058](0058-api-keys-and-service-accounts.md)): `TestPrincipalRestrict`, `TestMiddlewareAPIKeys`, `TestAPIKeysNeverCarryTwoFactorPermissions`, `TestAPIKeyScopesCantEscalate`, `TestUserRoleIsScopedLikeAnyRole`, `TestServiceAccountsNeedOpsPermissions`, `TestOrganisationServiceAccounts`, `TestAPIKeyFailuresAreLimited`; apps `TestAPIKeyScopesCoverOwnData`, `TestServiceAccountsThroughOps`, full-multi `TestAPIKeysAndOrganisations` (joining and leaving need a session), `TestOrganisationsCantReachEachOthersServiceAccounts`. Residual risk accepted above |
| 47 Live observability and incidents | Done ([ADR-0064](0064-live-observability-and-incidents.md)): `TestMiddlewareRecordsRoutes` (150 requests with client-chosen paths, queries, hosts, forwarded addresses and methods: 5 series, no client value), `TestCollectorSeriesAreBounded`, `TestCollectorKeepsBoundedMinutesWhileTheSinkFails`, `TestDetectIncidentOncePerDeployment` (30 concurrent detections on 3 pools: 1 incident), apps `TestObservabilityStream` (401/403/422 on connect, 429 over the limit, sign-out ends the stream with `unauthorized`), `TestObservabilityStreamEnds` (max duration, session end, permission removal, shutdown), `TestIncidentsThroughOps` (viewer can't write; audit without titles; report without IP, user agent or metadata; Markdown escapes HTML), `TestIncidentReportSections` (sections omitted per permission), `TestEveryOperationAuthorizesFirst`. Residual risk accepted above |
| 14 OAuth attacks (GitHub) | Done ([ADR-0059](0059-github-sign-in.md)): `TestGitHubWebSignIn` (PKCE verifier checked by the fake; used, foreign-verifier and unknown codes refused), `TestGitHubEmails` (only a verified primary non-no-reply address), `TestGitHubSignIn` (new accounts unverified; existing verified and unverified accounts get `social_link_required` unchanged; other browser, Google state, stolen code refused; unverified GitHub account kept by cleanup), `TestGitHubAccountIsClaimedByEmail`, `TestGitHubLinkWhileSignedIn` and `TestGitHubSignInEndToEnd` (link needs the password, refused for API keys and cross-site posts; a callback in another browser `invalid_state`; after logout `unauthenticated`; another account's GitHub `identity_in_use`; the link signs nobody in), `TestSignInReturnsToTheDefaultAddress` (production app). Residual risk accepted above |
| 18 Privilege escalation to ops | API keys never reach `/ops`: ops roles require 2FA, which keys can't have (`TestAPIKeysNeverCarryTwoFactorPermissions`, [ADR-0058](0058-api-keys-and-service-accounts.md)); the new `/ops/flags`, `/ops/observability` and `/ops/incidents` operations authorise first (`TestEveryOperationAuthorizesFirst`), `/ops/service-accounts` in the auth module likewise (`TestServiceAccountsNeedOpsPermissions`) |
| 48 Missing organisation filter | Done, optional layer ([ADR-0061](0061-row-level-security.md)): `modules/postgres` `TestRowLevelSecurityFollowsTheContext` (one connection reused across contexts: none, actor, another organisation, `WithOrg`, bypass; `WITH CHECK` refuses writes into another organisation; one log line per bypass), `TestRowLevelSecuritySettingFailureDropsTheConnection`, `TestMigrateBypassesRowLevelSecurity`, `TestCheckRowLevelSecurity`, `TestWithoutRowLevelSecurityNeedsAReason`; full-multi `TestRowLevelSecurity` (policies on under a running app as a role without bypass: 404 across organisations, a query without `org_id` sees one organisation's rows, a forged insert refused, seed, migrate, `orgs_purge` and `auth_cleanup` work) and `TestRowLevelSecurityCoversOrganisationTables`; the whole full-multi suite passes with `GORBITAL_TEST_RLS=1`; cli `TestAddRLSWritesTheMigration`, `TestAddRLSRefuses`, `TestDoctorReportsRowLevelSecurity`, `ORB_E2E=1` `TestAddRLS`. Residual risk accepted above |
| 11 Localhost services (dev console) | Done for the dev console ([ADR-0065](0065-local-dev-console-apis.md)): `TestAllowedHost`, `TestConsoleRefusesOtherHosts` and `TestConsoleRefusesMissingHost` (`evil.example`, `localhost.evil.example`, `127.0.0.1.nip.io`, other ports, IPv6 variants, missing Host: 403), `TestConsoleRequiresToken`, `TestCheckToken` and `TestValidTokenComparesHashes` (401), `TestConsoleRefusesRemotePeers`, `TestConsoleHeaders` (no `Access-Control-*`, `no-store`); apps `TestDevConsoleSecurity` in all three golden apps; cli `TestDevConsoleTokenPerRun` (new per run, in no file). Accepted above: the token is readable by other processes of the same OS user |
| 49 Dev console data exposure | Done ([ADR-0065](0065-local-dev-console-apis.md)): `TestEnvKeys` and apps' `TestDevConsoleConfigHidesSecrets` (real database URL, encryption keys and provider secrets only listed as set), `TestRequestsNeverHoldQueriesOrHeaders`, `TestLogsAreBounded`, `TestBuffer`, `TestStreams`, `TestStreamReportsDroppedEvents`, apps' `TestDevConsoleOnlyInDevelopment` (production with a token and short tokens refused; without a token `/_dev/app` is the app's 404; `/openapi.json` never mentions `_dev`). Residual risk accepted above |
| 17 Cross-tenant data access | Optional fifth layer, row-level security (row 48) |
