# v0.2 roadmap: a framework you import

**Status:** Proposed (2026-09-17), second draft after architecture review; route annotations removed (2026-09-17) · **Target release:** `v0.2.0` · **Branch:** `v0.2` · **Builds on:** [ADR-0002](adr/0002-module-architecture.md), [ADR-0015](adr/0015-public-api-and-stability-tiers.md), [ADR-0017](adr/0017-application-lifecycle.md), [ADR-0019](adr/0019-module-dependency-rules.md), [ADR-0021](adr/0021-generator-operation-model.md), [ADR-0022](adr/0022-generated-application-layout.md)

`v0.1.0` gives an app its features by generating them into the app. `v0.2.0` moves the reusable part of that code into the library, so an app imports a working server and writes only its own modules:

```go
func main() {
	gorbital.Main(
		gorbital.WithAuth(authhttp.New()),               // sign-in: sessions, MFA, passkeys, social, API keys
		gorbital.WithModules(opshttp.Module(), modules.All()...), // /ops, then your modules
	)
}
```

```go
Routes: func(r *gorbital.Router, d gorbital.Deps) {
	h := &handler{db: d.DB, audit: d.Audit}
	books := r.Group("/v1/books")                    // every route requires sign-in unless marked public
	gorbital.Post(books, "", h.createBook,
		guard.Permission(PermWrite), guard.RateLimit(30, time.Minute))
},
```

## Contents

1. [Current state](#current-state): what exists and what the actual problem is
2. [Goals and non-goals](#goals-and-non-goals)
3. [Architecture](#architecture): layers, package responsibilities, API tiers, lifecycle, failure modes
4. [Decisions](#decisions): what was chosen, and the alternatives rejected
5. [Engineering standards](#engineering-standards): quality gates every phase passes
6. [Documentation and versions](#documentation-and-versions): frozen v0.1 docs, the Methods and Examples tabs
7. [Phases](#phases) 0–12
8. [Not in v0.2](#not-in-v02): ideas considered and deliberately deferred
9. [Risks](#risks)
10. [Changes from the first draft](#changes-from-the-first-draft)

---

## Current state

Assessed on 2026-09-17 against `main` (`41f7c6b`), the golden apps and `portal-demo`.

| Finding | Evidence | Consequence |
|---|---|---|
| The library already has the building blocks | `httpx` (Recover, RequestID, TrustedProxies, AccessLog, SecureHeaders, CORS, CrossOrigin, BodyLimit, problem+json), `ratelimit` and `ratelimitpg`, `auth` (hashing, tokens, TOTP, API keys, `Middleware`, `Catalog`), `actor`, `idempotency`, `orgs`, `postgres`, `jobs`, `settings`, `flags`, `mail`, `storage`, `telemetry`, `observability` | v0.2 is mostly **composition and extraction**, not new behaviour |
| The wiring and the endpoints live in each app | `portal-demo/internal/app`: 10 027 lines (the middleware chain in `routes.go`, construction in `app.go`, `rate_limits.go`, `maintenance.go`, `permissions.go`); `internal/modules/{auth,ops,flags,mailevents}`: 16 963 lines; auth alone has 74 operations | Fixes reach apps only through `orb upgrade`'s three-way merge; every app carries ~27 000 lines it didn't write |
| Registering a module takes edits in four places | `internal/modules/<name>/module.go`, `internal/app/module_<name>.go` (error mappings), a line after `//orb:anchor modules` in `modules.go`, permissions in `permissions.go`; each operation is a verbose `huma.Register` with a hand-written `signedIn` wrapper | The boilerplate the user asked to remove |
| Protection is opt-in per operation | A route without the `signedIn` wrapper is public; the global per-IP limit covers `/v1/auth/` only | A forgotten wrapper is a security bug |
| Migrations are one ordered history | Library migrations are copied into `db/migrations` (`20260914000001_settings.sql` …), applied by goose from a separate command, never at startup ([ADR-0017](adr/0017-application-lifecycle.md)) | Moving modules into the library must keep the same version numbers so existing databases see no change |
| Jobs can be enqueued inside a transaction | `jobs.Client.InsertTx(ctx, tx, args, opts)` | Transactional side effects don't need a new event bus |
| Quality gates exist | CI: gofmt, vet, `go test -race`, golangci-lint, govulncheck, gitleaks, `apicheck`, `refdocs`, OpenAPI files match golden apps, new apps build and pass tests, scaffold compatibility | Reuse them; add what's missing: fuzz tests (none today), benchmark budgets (7 benchmark files, no budgets), v0.1-app-against-v0.2-library compatibility |

**The actual problem**, restated: apps own framework code they shouldn't have to own, adding a route takes too many edits, and protection depends on remembering a wrapper. The request for "Nest-style automatic routes" is one answer to the second part; v0.2 solves all three with an explicit API, and route annotations are deferred ([Not in v0.2](#not-in-v02)).

## Goals and non-goals

**Goals**

1. A new Full app's own code is its modules, `main.go` and migrations; no framework wiring.
2. A route is one call with its protection next to it; routes are **deny by default**.
3. Every sign-in feature of v0.1 comes from the library, with hooks for common changes and ejection for everything else.
4. **Existing v0.1 apps keep compiling against the v0.2 library without changes.** Converting to the new layout is opt-in (`orb upgrade`).
5. The HTTP contract of every built-in endpoint (paths, bodies, error codes, audit actions, permissions) is unchanged.
6. v0.1 documentation stays published unchanged; every v0.2 change is documented, including every public method and a real application using it.

**Non-goals for v0.2**

- A dependency injection container, reflection-based wiring or runtime registration by `init()`.
- File-based routing.
- A message bus, Redis or any second required datastore (PostgreSQL stays the only requirement, [architecture principle 5](architecture.md#2-principles)).
- New product features beyond the [committed security layers](#phase-10-security-layers) and the [Dev Portal tunnel and live sign-in tests](#phase-11-dev-portal-tunnel-and-live-sign-in-tests), added at the maintainer's request (2026-09-17).

## Architecture

### Layers

```text
your app (main.go, modules/, db/migrations)
   │ imports
   ▼
gorbital.dev/gorbital ─── gorbital.dev/gorbital/guard           ← NEW: composition library
   │ imports                                   (the only package that combines several modules)
   ▼
gorbital.dev/modules/*  (auth/authhttp, ops, flags/flagshttp, orgs/orgshttp, jobs, postgres, …)
   │ imports                                   (modules never import each other; unchanged)
   ▼
gorbital.dev  core      (httpx, ratelimit, actor, audit, mail, config, page, health, app)
   │ imports                                   (stdlib, OpenTelemetry API, golang.org/x; unchanged)
   ▼
standard library
```

The only rule that changes is at the top: `gorbital.dev/gorbital` may import several modules, as `internal/app` does today. It does not import optional drivers: storage backends, mail providers, `authhttp` and `orgshttp` are passed in by the app, so an app compiles only what it uses and the dependency graph of a small app stays small.

### Package responsibilities

| Package | Owns | Does not own |
|---|---|---|
| `gorbital.dev/gorbital` | Config loading, construction order, the default middleware stack, the router over Huma, `Module` and `Deps`, commands (`serve`, `migrate`, `openapi`, role commands), lifecycle via `app.Run` | Business rules, SQL, sign-in logic |
| `gorbital.dev/gorbital/guard` | Route-level allow/deny decisions and their OpenAPI description | Sessions, rate-limit storage |
| `modules/auth` (existing) | Building blocks: hashing, tokens, TOTP, API keys, `Middleware`, `Catalog` | HTTP endpoints |
| `gorbital.dev/gorbital/authhttp` (new) | The 74 sign-in operations, their use cases, SQL and migrations; options and hooks | Routing or middleware of other modules |
| `gorbital.dev/gorbital/opshttp`, `flagshttp`, `mailevents`, `orgshttp` (new) | Today's generated endpoints of the same names. They live in the composition module because they return `gorbital.Module`, and modules may not depend on the layer above ([ADR-0083](adr/0083-modules-stack-migrations-and-ejection.md)) | — |
| The app | Its modules, migrations, configuration values, which built-in modules it mounts | Framework wiring |

### API tiers

| Tier | Where | Promise |
|---|---|---|
| Public | `gorbital.dev/gorbital`, `guard`, new `*http` packages, new `httpx` functions | Listed by `apicheck`; breaking changes only with an ADR, changelog and upgrade notes (v0) |
| Experimental | `gorbital.dev/x/...` | May change in any release |
| Internal | `internal/` | None |
| Generated | `internal/modules/modules.gen.go` | Regenerated freely; never edited; the generator's output format is versioned with `orb` |
| Owned scaffold | `cmd/api/main.go`, `internal/modules/<name>/*`, ejected modules | The developer's code; `orb` never overwrites it without plan → diff → apply ([ADR-0021](adr/0021-generator-operation-model.md)) |

### Lifecycle

`gorbital.Main` is a thin, documented convenience over explicit steps an advanced app can call itself:

```text
gorbital.Main(opts...)
  ├─ parse subcommand            serve (default) · migrate · migrate-down · openapi · version · commands of built-in modules (roles, grant-role, … from Phase 5)
  ├─ gorbital.LoadConfig(config.OS) → (Config, error)          no global state; errors name the variable
  ├─ gorbital.New(ctx, cfg, opts...) → (*App, error)
  │     telemetry → postgres pool → audit → settings & flags registries (collected from modules)
  │     → stores → jobs → mail → auth → modules' Routes → middleware stack → handler
  ├─ (*App).Run(ctx)            app.Run: server + job workers + release heartbeat; signals; drain; shutdown order
  └─ exit code                  0 ok · 1 runtime error · 2 usage or configuration error
```

Migrations run only from `migrate` ([ADR-0017](adr/0017-application-lifecycle.md)). `(*App).Handler()` exists for tests and for mounting inside another server.

### Failure modes

| Failure | Behaviour |
|---|---|
| Invalid or missing configuration | `LoadConfig` returns every problem at once, naming the variable; exit code 2 |
| Two modules declare the same route, permission, setting, flag, job or error code | `New` fails before listening, naming both modules |
| A route has no auth decision | Impossible by construction: routes require sign-in unless `guard.Public()` |
| Database unreachable at start | `New` fails with the connection error (redacted URL); readiness stays false while it's down at runtime |
| Pending migrations | `New` starts and logs a warning; `migrate --status`, the dev console and `orb doctor` name them. Readiness stays a database ping, as in v0.1: failing it would take every instance of a rolling deploy out of rotation |
| A hook (`AfterLogin`) fails | Logged with the request ID; the sign-in succeeds |
| A hook (`BeforeLogin`, `OnRegister`) fails | The sign-in or registration is refused; `OnRegister` rolls back its transaction |
| A custom middleware stack omits recovery or authentication | Allowed (it is explicit code), with a startup warning and an `orb doctor` finding |
| Rate-limit store unavailable | Falls back to in-memory limits (today's `ratelimitpg` behaviour) and logs once |

## Decisions

Each becomes part of an ADR in Phase 0 and can be changed there; after Phase 0, a change needs a new ADR.

| # | Decision | Chosen | Rejected, and why |
|---|---|---|---|
| D1 | Import path | `gorbital.dev/gorbital`, its own Go module; call sites read `gorbital.Main`, `gorbital.Post` | `gorbital.dev/kit` (reads worse); growing core (its dependency budget forbids Huma and pgx) |
| D2 | Compatibility | **Additive.** No exported identifier of v0.1 changes meaning or is removed; v0.1 apps build against v0.2 (a CI job proves it). `orb upgrade` to the new layout is opt-in | A breaking v0.2 that forces every app to convert |
| D3 | Built-in features in `main.go` | **Explicit one-liners**: `gorbital.WithAuth(authhttp.New(...))`, `gorbital.WithModules(opshttp.Module(), …)`, `gorbital.WithStorage(s3.New(...))`. `orb new` writes them | Everything on implicitly (hides what an app contains, pulls every dependency into every app) |
| D4 | Construction | `LoadConfig` → `New` → `Run`, plus `Main` as convenience | Only `Run(cfg)` (hard to test and to embed) |
| D5 | Dependencies for modules | `gorbital.Deps`, a plain struct with documented, possibly-nil fields | A DI container; reflection; a service locator with `Get[T]` |
| D6 | Route declaration | Explicit `gorbital.Get/Post/…` inside `Module.Routes` is the primary API. Generic package-level functions, because typed Huma handlers can't be generic methods | Only annotations; file-based routing (one package per URL segment, nothing to "go to definition" on) |
| D7 | Nest-style route annotations | **Not in v0.2** (decided 2026-09-17). Routes are declared only with D6's explicit calls; `orb gen module` writes a new module's first routes | Annotations (`//gorbital:route`) generated into `routes.gen.go`: a comment grammar to support for years, a parser and a file watcher, and code in comments the compiler doesn't check, for a gain the explicit API mostly delivers |
| D8 | Module discovery | `internal/modules/modules.gen.go` written by `orb dev`/`go generate`, committed; the app builds without `orb` | `init()` self-registration (hidden) |
| D9 | Default protection | **Deny by default**: every route requires sign-in; `guard.Public()` opts out; `orb routes` lists public routes | Opt-in `guard.Auth()` (today's forgotten-wrapper bug) |
| D10 | Middleware | Every level takes `httpx.Middleware` (`func(http.Handler) http.Handler`). App: `WithMiddleware` (runs after authentication). Module: `Module.Middleware`. Group and route: `gorbital.Use`. Route-level middleware is adapted onto Huma with `humago.Unwrap`/`humago.NewContext` | A gorbital middleware type |
| D11 | Changing the built-in stack | `gorbital.WithStack(func(s gorbital.Stack) []httpx.Middleware)`: `Stack` has one field per built-in step; the function returns the order. Plain Go | Positional options `Before`/`After`/`Replace`/`Without` (a mini-language to learn) |
| D12 | Custom guards | `guard.New(guard.Spec{Name, Statuses, Check})`; `Check` returns an error mapped by the module's `Errors` | Guards writing responses directly |
| D13 | Module layout | **Layered by default, one file per operation** in each of `usecase/`, `repository/` and `delivery/`; the route table stays in `delivery/routes.go`; `domain/` holds types and errors; `module.go` wires the layers; ADR-0039's architecture rules hold (maintainer decision, 2026-09-17) | A flat layout (`module.go`, `handlers.go`, `store.go`): dropped, because operation files of different layers collide in one package (two `update_book.go`), and one layout for every module is the maintainer's preference |
| D14 | Migrations | One ordered goose history. Library modules number their files locally (`00001_settings.sql`) while v0.1 apps hold the same content as `20260914000001_settings.sql`, so `gorbital` keeps a frozen version table mapping each library file to its v0.1 app version; built-in modules declare theirs. `migrate` merges them with `db/migrations`: same version and content is one migration, a conflict fails naming both files. App modules keep migrations in `db/migrations` (cross-module foreign keys stay possible) | Per-module version tables (breaks ordering across modules, changes existing databases) |
| D15 | Sign-in customisation | Options; hooks `BeforeLogin` (can refuse), `AfterLogin` (errors logged), `OnRegister` (in the registration transaction); `authhttp.RegisterFields[T]`; `Deps.Auth.SignIn` for new sign-in methods, which applies bans, MFA, audit, rate limits and transport | Copying auth into apps |
| D16 | Escape hatch | `orb eject <module>` copies a library module into the app as owned, layered code | None |
| D17 | Minimal preset | Keeps composing core packages directly (no database); `gorbital.New` requires PostgreSQL. Re-evaluated in Phase 3 against a measured dependency and binary-size budget | A database-optional facade (branches in every constructor) |
| D18 | Docs versions | `release/v0.1` frozen and published at `/v0.1/`; v0.2 written on `v0.2`, previewed at `/next/` | One set of docs edited in place |

## Engineering standards

Every phase passes these before it is marked done. Existing CI jobs are reused; new ones are added in Phase 0.

| Area | Gate |
|---|---|
| Formatting and analysis | `gofmt`, `go vet`, golangci-lint (staticcheck, errorlint, gosec as configured), `govulncheck` on every module |
| Tests | Table-driven unit tests; `go test -race` everywhere; database tests against Docker PostgreSQL (`pgtest`); HTTP tests through the real handler |
| Contracts | `apicheck` listings for every new exported identifier; OpenAPI of the golden apps unchanged except where the phase says so; **the v0.1.0 OpenAPI of `/v1/auth/*`, `/ops/*` and `/v1/flags` is a frozen fixture**; error codes, audit actions, permissions and settings keys compared with `docs/reference` |
| Compatibility | A CI job builds and tests an app generated by `orb v0.1.0` against the branch's library (D2); `TestScaffoldCompatibility` keeps running |
| Fuzzing | New `Fuzz*` tests for every parser the phase adds: route paths and parameters, webhook signature headers, JWT headers, config values |
| Benchmarks with budgets | Recorded in `docs/benchmarks.md`, compared in CI with `benchstat` on the phases that touch them: route adapter overhead versus a direct `huma.Register` (target ≤ 1 extra allocation, ≤ 5 % latency); each guard on the allow path (0 allocations except rate limits); `gorbital.New` start time and memory versus v0.1 `full-single` (≤ +10 %); `go list -deps` count and binary size of the golden apps (no growth without a note) |
| Security | A threat model section in the phase's ADR for anything touching authentication, authorization, sessions, webhooks, tokens or uploads; findings fixed or recorded in `docs/security/` |
| Observability | Spans named by route pattern; guard refusals counted by guard name and route pattern (bounded cardinality); hooks traced; no per-request logs added at INFO |
| CLI | Every new command has `--json`, works with `--no-input`, documents its exit codes, and has golden tests for text and JSON output |
| Generated code | Compiles, passes `gofmt` and golangci-lint in a generated app, reads as hand-written Go, and is covered by golden files |
| Review | Each phase ends with a self-review against the [architecture review rules](#architecture) and a list of known gaps in this page's status table |

## Documentation and versions

Documentation ships with each phase, not after.

### Branches

| Branch (gorbital and gorbital-web) | Holds | Changes allowed |
|---|---|---|
| `release/v0.1` | Code and docs of `v0.1.0` as published (gorbital `main` at `41f7c6b`) | Security fixes and factual corrections, cherry-picked and listed in the v0.1 changelog |
| `v0.2` | The v0.2 work; phase branches merge into it | This roadmap |
| `framework/phase-N` | One phase, stacked on the previous | That phase's items |
| `main` | The latest release | Moves to `v0.2` at release (Phase 12) |

The branch `v0.2-finish` predates the public release: it is the internal development milestone once called v0.2, unrelated to this release line.

### The docs site

| URL | Content | Source |
|---|---|---|
| `docs.gorbital.dev/…` | Latest release: v0.1 until v0.2.0 ships, then v0.2 | `main` |
| `docs.gorbital.dev/v0.1/…` | Always v0.1; links never break | `release/v0.1` |
| `docs.gorbital.dev/next/…` | Unreleased v0.2, with a "not released yet" banner | `v0.2` |

A version switcher in the header keeps the reader on the same page in the other version when it exists; older versions show a banner linking to the latest.

### What every phase writes

| Deliverable | Where | Enforced by |
|---|---|---|
| ADR (new or updated), with alternatives and, when relevant, a threat model | `docs/adr/` | Review, link check |
| Guides for what developers do | `docs/guides/`, `docs/sign-in/`, `docs/start/` | Docs build |
| **Methods** entries for every exported identifier added or changed | Generated into `docs/methods/` by `internal/tools/refdocs` from doc comments and `Example` functions, with curated overlay text | `refdocs -check` fails on an exported identifier without a doc comment and an `Example` |
| **Examples** chapter or recipe using the phase's features | Pages in `docs/examples/`, code in `examples/apps/` | Example apps build and pass tests in CI; pages include code by marker, so text can't drift from code |
| Changelog | `CHANGELOG.md` `Unreleased (v0.2.0)` | Review |
| Upgrade notes | `docs/guides/upgrade-notes.md` `Upgrading to v0.2.0` | Review |
| Navigation and roadmap status | `docs/docs.json`, this page | Docs build |
| Synced site | gorbital-web `pnpm sync-docs` on the matching branch | Preview deploy of `/next/` |

### The Methods tab

One page per package. For each function, type, option and error: signature; what it does; parameters; return values and **which errors it returns and when**; a runnable example; the version it arrived in; related methods; the guide that explains it. Guides link to Methods entries wherever they name a method.

### The Examples tab

Real applications, built in order and explained step by step. Code on each page is included from the compiled example app.

**Shelfie**, a reading-tracker API for a web and mobile app:

| Chapter | Shows | Phase |
|---|---|---|
| 0. Start a project | `main.go` with `gorbital.Main`, migrations, the module list, `.env`, `orb dev` (`orb new` writes the layout from Phase 9) | 3 |
| 1. A books module | `Module`, the layers with one file per operation, `delivery/routes.go`, a migration, error mappings | 1 |
| 2. Protecting routes | Deny by default, `guard.Permission`, `guard.RateLimit`, `guard.RecentReauth`, `guard.Public` | 2 |
| 3. Your own middleware and guards | `requireClientVersion`, context values, `httpx.Capture`, a subscription guard with `guard.New` | 2 |
| 4. Tests | `gorbitaltest`: signed-in requests, problem assertions, captured mail and jobs | 3 |
| 5. Operations | `/ops`, runtime settings and flags declared by a module | 4 |
| 6. Accounts | `authhttp` options, `BeforeLogin`/`AfterLogin`/`OnRegister`, extra registration fields | 6 |
| 7. Phone-code sign-in | A custom sign-in method through `Authenticator.SignIn` | 6 |
| 8. Book clubs | Organisations, `guard.OrgMember`, row-level security | 7 |
| 9. Generators | `orb gen module`, `orb gen middleware`, `orb routes` | 8 |
| 10. Hardening and partners | Timeouts, `/ops` IP allow list, verifying partner webhooks | 10 |
| 11. Deploy | Production configuration, migrations in CI, health checks | 12 |

**Recipes**, each a small runnable app: *Multi-tenant invoicing* (Phase 7), *Internal admin tool* (Phases 4, 10), *Mobile backend with an external identity provider* (Phase 10, JWT), *Receiving payment webhooks* (Phase 10, signatures, idempotency, `InsertTx`), *Upgrading a v0.1 app* (Phase 9).

---

## Phases

Order follows dependency and risk: the explicit API first, then the app, then extracting modules from least to most sensitive (ops before auth), then generators on a stable base, then upgrades, then new security layers.

| Phase | Name | Status | Branch |
|---|---|---|---|
| [0](#phase-0-decisions-branches-docs-and-gates) | Decisions, branches, docs and gates | Done (2026-09-17), except maintainer actions: protect `release/v0.1` on GitHub (item 1), the Vercel preview of `/next/` (item 18). Fuzzing found and fixed two bugs (`mail.RedactAddresses`, `auth.NormalizeRecoveryCode`) | `framework/phase-0` |
| [1](#phase-1-router-module-and-deps) | Router, Module and Deps | Done (2026-09-17): items 19–26, 28–30; `NewID` and `Auditor` dropped, jobs moved to Phase 3 (item 27). Shelfie chapters start with Phase 3, when an example app can run end to end with `gorbital.Main` | `framework/phase-1` |
| [2](#phase-2-guards-and-middleware) | Guards and middleware | Done (2026-09-17): `guard.Role` and `guard.Idempotent` not built (ADR-0082); rate limiters appear in `/ops/auth/rate-limits` with Phase 4, when `/ops` moves into the library; Shelfie chapters 2–3 with Phase 3 | `framework/phase-2` |
| [3](#phase-3-the-app-config-lifecycle-and-test-kit) | The app: config, lifecycle and test kit | Done (2026-09-17): items 42–52. Notes ([ADR-0083](adr/0083-modules-stack-migrations-and-ejection.md#phase-3-implementation-notes-2026-09-17)): options `WithStorageFunc`, `WithMailerFunc`, `WithMigrations`, `WithName` added; provider-specific configuration checks moved to the providers (sign-in's to Phase 5); the golden apps stay on v0.1 wiring and Shelfie proves `Main` instead (item 50); D17 stands. Known gaps: the dev operator and email previews of the dev console wait for Phases 4–5; `gorbital.New` start time can't be compared with `full-single` like for like until sign-in and `/ops` are modules (Phases 4–5); the Dev Portal's schema view reads `db/migrations` only, not the library's merged migrations; two apps built at once in one process race on Huma's package-level error constructor (`openapi.InstallErrors`), so tests that build apps can't use `t.Parallel()` | `framework/phase-3` |
| [4](#phase-4-ops-flags-and-mail-events-in-the-library) | Ops, flags and mail events in the library | Done (2026-09-17): items 53–57, and `OPS_ALLOWED_IPS` (item 90) and the dev operator on `/ops` left from Phases 3 and 10. Notes ([ADR-0083](adr/0083-modules-stack-migrations-and-ejection.md#phase-4-implementation-notes-2026-09-17)): built-in modules reach what the app built through `Module.Platform` and the `Platform` type, not `Deps`; `Module.RateLimiters` and `Module.Retention` added (item 55); `gorbital.Customize` added for response media types; mail events keep their verifier rather than `guard.Webhook`; the golden apps stay on v0.1 wiring and their `/ops`, flags and webhook HTTP tests run against the library modules. What Phase 5 had to provide (`/ops/auth/users` and `/ops/service-accounts`, `ops.auth.write`, `RequireMFA` for the ops roles, the sign-in methods report) was integrated with it on `framework/phase-5` (see Phase 5) | `framework/phase-4` |
| [5](#phase-5-sign-in-extracted-unchanged) | Sign-in extracted, unchanged | Done (2026-09-17): items 58–63. Notes and threat model ([ADR-0083](adr/0083-modules-stack-migrations-and-ejection.md#phase-5-implementation-notes-moving-sign-in-2026-09-17)): `authhttp.New()` with `Middleware`, `Module`, `Commands`, `CheckConfig` and `Setup`; `gorbital.AuthSetup` hands it the configuration, `Deps` and permission catalog (from `New`, and with zero `Deps` from `Main` before its commands); the dev operator, sign-in's email previews and the passkey `.well-known` files are wired by `gorbital.New`; the golden app's sign-in HTTP, use-case and repository tests run against the library; the 66 operations of the frozen OpenAPI and the schemas they use are identical apart from `x-gorbital-guards`. No HTTP behaviour changed: signed-in sign-in routes keep v0.1's order of responses through an internal route option. Outside HTTP: sign-in's configuration errors come after `LoadConfig`'s, wrong command arguments exit 2, the sign-in methods are logged at start instead of printed. Integrated with Phase 4 (2026-09-17, [ADR-0083](adr/0083-modules-stack-migrations-and-ejection.md#integration-of-phases-4-and-5-2026-09-17)): `/ops/auth/providers` lists the authenticator's methods (`gorbital.SignInMethod`, `Platform.SignInMethods`; `opshttp.SignInMethods` removed), `authhttp` declares its rate limiters and the accounts rows of `/ops/retention`, `ops.auth.read` and `ops.auth.write` are declared once by `authhttp`, and `gorbital/internal/integration` proves the four modules together with real sign-in and against the v0.1.0 contracts (`gorbitaltest.NewWithEnv` added for it). Known gaps: in an app with `opshttp` and no `authhttp`, no role holds `ops.auth.read`/`ops.auth.write`; `/ops/auth/rate-limits` lists `ops_test_email` last instead of before `auth_api_key`; requiring a second factor for roles other than `platform_admin` and `ops_viewer` waits for Phase 6's options; the golden apps stay on v0.1 wiring (Phase 9); organisations' service accounts are moved but not mounted (Phase 7); no seed command on `Main` | `framework/phase-5` |
| [6](#phase-6-sign-in-options-hooks-and-custom-methods) | Sign-in options, hooks and custom methods | Done (2026-09-17): items 64–70. Notes, decisions and threat model ([ADR-0083](adr/0083-modules-stack-migrations-and-ejection.md#phase-6-implementation-notes-sign-in-options-hooks-and-custom-methods-2026-09-17)): `authhttp.New(opts...)` (v0.1's sign-in without options; the frozen v0.1.0 contract unchanged) with `MinPasswordLength`, `PasswordPolicy`, `RequireMFA`, `APIKeyMaxTTL`, `WithoutRegistration` (new code `registration_closed`), `Brand`, `RouteMiddleware`; hooks `BeforeLogin` (after every factor and the ban check, in the session's transaction; `Refuse` with codes checked against every built-in code), `AfterLogin` (bounded, errors logged), `OnRegister` (in the account's transaction for registration, first provider sign-ins and operators; registration still answers 202), `RegisterFields[T]`; `Authenticator.SignIn` and `User` for modules' own methods, which take the authenticator as an argument instead of a `Deps.Auth` field; challenge tokens of non-password sign-ins carry the method under their hash. Dropped: `Prefix`, `Passkeys`/`Google`/`Apple`/`GitHub` code options, `MFA(TOTP, RecoveryCodes)` toggles (reasons in the ADR). Shelfie chapters 6 (profiles, a default shelf, suspended readers) and 7 (phone-code sign-in). Known gaps: password field descriptions follow `MinPasswordLength` (fixed 2026-09-17); service accounts don't run `OnRegister`; `AfterLogin` hooks that ignore their context outlive the response | `framework/phase-6` |
| [7](#phase-7-organisations-and-tenancy) | Organisations and tenancy | Done (2026-09-17): items 71–75. Notes, decisions and threat model ([ADR-0083](adr/0083-modules-stack-migrations-and-ejection.md#phase-7-implementation-notes-organisations-and-tenancy-2026-09-17)): `orgshttp.Module(auth, opts...)` takes the app's `*authhttp.Authenticator` (organisation members are sign-in's accounts) instead of `Module(opts...)`, and mounts organisations' service accounts through `authhttp.UseOrganisations` and `OrgServiceAccountRoutes`; `guard.OrgMember` asks the app's `gorbital.OrgAuthorizer` (`Platform.SetOrgAuthorizer`), and organisation permissions are declared with `gorbital.Permission.OrgRoles` in a second catalog; `gorbitaltest.App.SignUp` for tests with real accounts; `orb gen module --org` (conditional foreign key to `orgs`, policy when the app has a `*_row_level_security.sql` migration); contract tests against full-multi's frozen v0.1.0 OpenAPI and surface (one anonymous body schema name differs, `CreateInputBody`); Shelfie chapter 8 and the *Multi-tenant invoicing* recipe. Known gaps: `orb add rls` needs a v0.1 lock, so apps on `gorbital.Main` add the policy migration by hand; the organisations module's v0.1 migration versions can't be applied to a database already migrated past them; no invitation email preview in the dev console; `orgshttp` requires `authhttp`; the Dev Portal's built UI still shows the module form's organisation option disabled | `framework/phase-7` |
| [8](#phase-8-generators-and-cli) | Generators and CLI | Built (2026-09-17): items 76–82 ([ADR-0083 notes](adr/0083-modules-stack-migrations-and-ejection.md#phase-8-implementation-notes-2026-09-17)); Shelfie's `shelves` module is `orb gen module`'s golden output, chapter 9 written. Decisions: `--org` refused until Phase 7 (the owner-scoped module is generated; generated since Phase 7), `orb gen resource` runs `orb gen module` in apps on `gorbital.Main`, middleware is never wired by editing files (the line is printed), optional `string?` fields. Known gaps: `orb routes` finds sources only for literal paths and groups within one function, lists no guards for v0.1 apps and no source for library routes; the stack check reads function literals and package functions only; no sqlc variant; Dev Portal pages have no new screenshots | `framework/phase-8` (gorbital, gorbital-dashboards) |
| [9](#phase-9-upgrade-eject-and-new-apps) | Upgrade, eject and new apps | In progress. Item 83 and the Getting started pages of item 88 built (2026-09-17, [ADR-0083 notes](adr/0083-modules-stack-migrations-and-ejection.md#phase-9-implementation-notes-new-apps-on-the-v02-layout-2026-09-17)): `orb new --preset full` writes apps on `gorbital.Main`; the v0.1-layout golden apps and templates are kept (`examples/v0.1`, `recipes/full`), `gorbital.lock` records the layout, and `orb upgrade`, `orb add orgs` and `orb add mail` use the app's own layout's templates, so v0.1 apps keep theirs. Decisions: `api/surface.json` records the app's own names only; `authhttp` adds a `seed` command; `WithStorageFunc` may keep the built-in store; `Platform.Permissions` added; D17 stands. Known gaps: `orb add orgs` in a v0.2 app can't migrate an existing database (the organisations module's migration versions); it now warns and gives both routes (recreate the database, or apply the two files and record them in `goose_db_version`); no `maintenance` command on `Main`; `orb gen job` is v0.1-only | `framework/phase-9-templates` |
| [10](#phase-10-security-layers) | Security layers | Done (2026-09-17): items 89–94; the `Timeout` stack step (`APP_REQUEST_TIMEOUT`) and `gorbital.Timeout` integrated with Phase 3, `OPS_ALLOWED_IPS` with Phase 4, the docs of item 94 with Phase 12. Built: `gorbital.dev/httpx/timeout`, `gorbital.dev/httpx/ipfilter` (first in `httpx`, moved on `framework/phase-5` because v0.1.0 apps record the problem codes of the packages they link: [ADR-0085](adr/0085-security-layers.md#package-moves-2026-09-17)), `gorbital.dev/webhook` with `guard.Webhook`, `modules/jwt`; no replay-ID store (idempotent handlers). Shelfie [chapter 10](examples/shelfie/10-hardening.md) and the recipes [Receiving payment webhooks](examples/recipes/receiving-payment-webhooks.md) and [Mobile backend with an external identity provider](examples/recipes/mobile-backend-with-an-idp.md) written in Phase 12 | `framework/phase-10` |
| [11](#phase-11-dev-portal-tunnel-and-live-sign-in-tests) | Dev Portal tunnel and live sign-in tests | Tunnel built (2026-09-17): items 95–98 and the tunnel half of 101 ([ADR-0086](adr/0086-dev-portal-tunnel.md)). Decisions: the token reaches cloudflared as `TUNNEL_TOKEN` in its environment, not `--token` on its command line (visible in `ps`); the dev console and dev operator also refuse requests with forwarding headers (a named tunnel's *HTTP Host Header* setting can make tunnelled requests carry a local `Host`); a named tunnel isn't restarted when the app's port changes (its target is set in the Cloudflare dashboard), the status says where to point it; `orb dev` has no JSON output, the portal's `GET /_portal/api/tunnel` is the machine-readable status. Checked with a real quick tunnel (cloudflared 2026.3.0): `/livez` 200 and `/_dev/` 403 through it, no cloudflared left after `Ctrl+C`. Known gaps: a `SIGKILL` of `orb dev` on macOS leaves cloudflared running; no revert of `.env` when the tunnel stops; no Tunnel screenshots. "Test now" (items 99–100) not started | `framework/phase-11-tunnel` (gorbital, gorbital-dashboards) |
| [12](#phase-12-review-and-release) | Review and release | In progress. Items 102 and 103 done (2026-09-17, `framework/phase-12-12b`): the Examples tab complete — Shelfie's chapters 0 to 11 and five recipes, five apps under `examples/apps/` in CI — and every guide read against the code, with `internal/tools/docscheck` added so a broken link, anchor or include marker fails CI instead of the docs site's build. Known gaps closed with them: the documented password minimum, IPv4-mapped trusted proxies, the error codes of unlinked library modules, a cloudflared left by a killed `orb dev`, and `orb add orgs` on an existing v0.2 database. Items 104–111 open | `framework/phase-12` |

Each phase lives on `framework/phase-N`, stacked on the previous one, in every repository it touches (gorbital; gorbital-web for the docs site; gorbital-dashboards when the Dev Portal changes), and is built in the order design → code → tests → verification → docs. Items are numbered continuously so commits, tests and notes can name them.

### Phase 0: decisions, branches, docs and gates

**Goal:** the decisions are recorded, v0.1 docs are frozen and still published, the v0.2 docs site and its new tabs exist, and the new quality gates run, before any library code changes.

| # | Item | Repo |
|---|---|---|
| 1 | Branches `release/v0.1` (at `41f7c6b`) and `v0.2` in gorbital and gorbital-web; protect `release/v0.1` on GitHub (maintainer action) | both |
| 2 | ADR-0081 *A framework you import*: current state, goals, layers, API tiers, D1–D5, D17 | gorbital |
| 3 | ADR-0082 *Routes, guards and middleware*: D6, D7 (why annotations are not in v0.2), D9–D12, with a threat model for deny-by-default and guard ordering | gorbital |
| 4 | ADR-0083 *Built-in modules, migrations and ejection*: D8, D13–D16 | gorbital |
| 5 | ADR-0084 *Versioned documentation*: D18, Methods and Examples tabs, documentation rules | gorbital |
| 6 | Revise [architecture principle 3](architecture.md#2-principles) to "library for behaviour and default wiring; generation for the module list and scaffolding" (on `v0.2` only) | gorbital |
| 7 | `sync-docs` reads each version from its git ref into `apps/docs/content/<version>/`; `versions.json` (label, ref, prefix, status) | gorbital-web |
| 8 | Routes `/v0.1/…`, `/next/…` and unprefixed latest; per-version sitemap and canonical URLs | gorbital-web |
| 9 | Version switcher and version banners | gorbital-web |
| 10 | Methods and Examples tabs, rendered only for versions whose `docs.json` has them (v0.1 unchanged) | gorbital, gorbital-web |
| 11 | Include-by-marker in the Markdown pipeline, failing the build on a missing marker | gorbital-web |
| 12 | `refdocs` generates `docs/methods/<package>.md` from doc comments and `Example` functions; `-check` in CI | gorbital |
| 13 | `examples/apps/` in the workspace; CI job building and testing every example app | gorbital |
| 14 | CI: a v0.1.0-generated Full app built and tested against the branch's library (D2) | gorbital |
| 15 | CI: fuzz job (short run per `Fuzz*` on pull requests, longer nightly); `benchstat` comparison job; `docs/benchmarks.md` with today's baselines (start time, memory, dependency count and binary size of the golden apps) | gorbital |
| 16 | Frozen fixtures: v0.1.0 OpenAPI of `/v1/auth/*`, `/ops/*`, `/v1/flags`, `/v1/webhooks/resend`; `docs/reference` of v0.1.0 | gorbital |
| 17 | `CHANGELOG.md` `Unreleased (v0.2.0)`; upgrade-notes stub; `CONTRIBUTING.md` section on documenting methods and writing examples; pull request template checklist (ADR, Methods, example, changelog, upgrade notes) | gorbital |
| 18 | Preview deploy of `/next/` from `v0.2` (Vercel) | gorbital-web |

**Done when:** docs.gorbital.dev is unchanged; `/v0.1/` and `/next/` work; the new CI jobs pass on `v0.2`; the ADRs are accepted.

### Phase 1: Router, Module and Deps

**Goal:** a module declares everything about itself in one value, with explicit routes that are protected by default.

| # | Item |
|---|---|
| 19 | New Go module `gorbital.dev/gorbital`; added to the release script, `apicheck`, golangci-lint and govulncheck matrices, and the architecture test (the one package allowed to import several modules) |
| 20 | `Router` over a `huma.API`: `Group(prefix, opts...)`; nested groups inherit options |
| 21 | Generic `Get`, `Post`, `Put`, `Patch`, `Delete` for typed Huma handlers; options `Summary`, `Description`, `Status`, `Tags`, `OperationID`, `Errors`, `Deprecated`; operation IDs default to the module's name plus Huma's generated ID, summaries to Huma's |
| 22 | Deny by default: every operation requires an authenticated actor unless `guard.Public()` (the minimal `guard` package lands here with `Public` and the implicit authentication check); OpenAPI marks security accordingly |
| 23 | `Module`: `Name`, `Routes func(*Router, Deps)`, `Errors []httpx.Mapping`, `Permissions`, `Settings func(*settings.Registry)`, `Flags func(*flags.Registry)`; `Middleware` arrives with Phase 2, `Jobs` and `Migrations` with Phase 3 (the job client needs its definitions before it exists, so their order is `New`'s); `Declare` and `Grants` for permissions, settings and flags; `Mount` for errors and routes |
| 24 | `Deps`: `DB`, `Audit` (`audit.Recorder`), `Mailer`, `Jobs`, `Settings`, `Flags`, `Storage`, `Logger` tagged with the module (documented nil when not configured); what sign-in exposes is decided in Phase 6 |
| 25 | Duplicate detection naming both modules: module names, routes (parameter names ignored), operation IDs, permissions; settings, flags and error mappings through their registries' own checks, reported with the module's name |
| 26 | OpenAPI export without a database (modules built with zero `Deps`, as today's nil-module path) |
| 27 | ~~`NewID(prefix)`; `Auditor`~~ Dropped (2026-09-17): `auth.NewID` and `audit.Recorder` already exist; a second name for each adds API without a reason |
| 28 | Tests: routing and groups; deny by default (a route without `Public` answers 401, before input parsing); collisions; OpenAPI identical to the equivalent hand-written `huma.Register` with the v0.1 `signedIn` wrapper; fuzz of path joining; `Example` functions for every identifier |
| 29 | Benchmarks: route registration and request overhead versus direct Huma (budget in [standards](#engineering-standards)) |
| 30 | Docs: guide [Modules and routes](guides/modules-and-routes.md) (including modules in a v0.1 app); Methods pages for `gorbital` and `guard`; Shelfie chapter 1 |

### Phase 2: guards and middleware

**Goal:** protection is one option per route; any standard middleware attaches at module, group or route level; custom guards are first-class.

| # | Item |
|---|---|
| 31 | `gorbital.Use(mws...)` for groups and routes; `Module.Middleware`; the `httpx.Middleware` → Huma operation adapter (no per-route removal of inherited middleware: use a separate group, ADR-0082) |
| 32 | `guard.Permission`, `guard.RecentReauth` (~~`guard.Role`~~: actors carry permissions, not roles) over `actor` and `auth.Catalog`, with today's error codes (`forbidden`, `mfa_required`) and one new code, `reauthentication_required` (ADR-0082) |
| 33 | `guard.RateLimit(n, window, opts...)` keyed per user, API key or IP (`ByUser`, `ByAPIKey`, `ByIP`), on `ratelimitpg` with its in-memory fallback; named limiters listed in `/ops/auth/rate-limits` |
| 34 | ~~`guard.Idempotent()`~~ Not built: the default stack keeps idempotency keys on every signed-in POST and PATCH, so a route option would add nothing (ADR-0082) |
| 35 | `guard.New(guard.Spec{Name, Statuses, Check})`; `guard.Request` with `PathParam`, `Query`, `Header`, `Operation`; errors mapped in the module's `Errors` |
| 36 | Guards describe themselves in OpenAPI: security requirements, 401/403/429 and custom error responses, `x-gorbital-guards` |
| 37 | `httpx.Capture(w)`: status, bytes and headers for middleware that reads the response (extracted from `AccessLog`) |
| 38 | Observability: guard refusals counted by guard name and route pattern; spans record which guard refused |
| 39 | Tests: order (app → module → group → route → handler); public; each guard's refusal as problem+json with request ID; OpenAPI output; race tests for concurrent limiter use |
| 40 | Benchmarks: allow path of each guard; adapter overhead with 0, 1 and 5 route middlewares |
| 41 | Docs: guides *Guards*, *Writing middleware*, *Writing a guard* (rules: call `next` once, errors as problem+json, context values with `From` functions, pass `r.Context()` to queries, never log secrets); Methods for `guard`, `Use`, `httpx.Capture`; Shelfie chapters 2 and 3 |

### Phase 3: the app, config, lifecycle and test kit

**Goal:** `gorbital.Main` builds and runs what `internal/app` builds today, and apps can be tested through the real stack from the start.

| # | Item |
|---|---|
| 42 | `LoadConfig(config.Source) (Config, error)`: every variable of today's `internal/app/config.go`, same names, validation and production refusals, all problems reported at once |
| 43 | `New(ctx, cfg, opts...) (*App, error)` in the order of [Lifecycle](#lifecycle); `(*App).Run`, `Handler`, `Close`; `Main(opts...)` with subcommands and exit codes |
| 44 | Options: `WithModules`, `WithAuth` (accepts a small `gorbital.Authenticator` interface so `gorbital` doesn't import `authhttp`; optional `Module()` and `Commands()`), `WithStorage` and `WithStorageFunc`, `WithMailer` and `WithMailerFunc`, `WithMiddleware` and `WithMiddlewareFunc`, `WithStack`, `WithLogger`, `WithMigrations`, `WithName` |
| 45 | The default stack as `gorbital.Stack` fields: Recover, TrustedProxies, RequestID, Telemetry, Observability, AccessLog, SecureHeaders, CORS, CrossOrigin, BodyLimit, Maintenance, Auth, RateLimit, Idempotency; `WithStack` with startup warning when Recover or Auth is omitted |
| 46 | `httpx.Maintenance(httpx.MaintenanceOptions)` moved from the app: the switch, message and `Retry-After` as `config.Value`s, and the paths kept open |
| 47 | `migrate` merging library migrations (through the frozen version table) and built-in modules' `Module.Migrations` with `db/migrations` by version (D14, ADR-0083); identical duplicates collapse, conflicts fail naming both files; same goose table as v0.1 |
| 48 | `internal/modules/modules.gen.go` generator (the module list only): `orb gen modules`, run by `orb dev` before each build when the file exists and by `go generate` through the file's own directive |
| 49 | `gorbital.dev/gorbital/gorbitaltest`: an `App` from modules on the test database, `As(User(...))`, `As(APIKey(...))`, request helpers, `AssertStatus`, `AssertProblem`, queued mail and enqueued jobs |
| 50 | Prove `gorbital.Main` with a separate app, `examples/apps/shelfie` (the golden apps are `orb new`'s templates and convert in Phase 9, and their HTTP tests exercise sign-in and `/ops`, which become modules in Phases 4–5); D17 re-evaluated with measured dependency count, binary size, start time and memory |
| 51 | Tests: every production refusal of today's `config.go`; shutdown order; exit codes; `migrate` on a v0.1 database is a no-op |
| 52 | Docs: guides *Your main.go*, *Configuration* (environment variables page), *The middleware stack* (replaces the stack section of *Request lifecycle*), *Testing with gorbitaltest*; Methods for all of the above; Shelfie chapters 0 and 4 |

### Phase 4: ops, flags and mail events in the library

**Goal:** the first extraction, on the least sensitive modules, proving the method used for sign-in next.

| # | Item |
|---|---|
| 53 | `gorbital.dev/gorbital/opshttp` from the generated ops module: use cases, endpoints, system and retention reporters; `opshttp.Module(opts...)`; what it reads from the app through `Module.Platform` (ADR-0083); the dev operator on `/ops` in development; `/ops` behind `OPS_ALLOWED_IPS` (item 90) |
| 54 | `gorbital.dev/gorbital/flagshttp` (`GET /v1/flags`) and `gorbital.dev/gorbital/mailevents` (`POST /v1/webhooks/resend`) as modules |
| 55 | Retention targets and rate limiters collected from modules (`Module.Retention`, `Module.RateLimiters`, and `guard.RateLimit`'s limiters) instead of listed in `app.go` |
| 56 | Contract tests against the frozen v0.1.0 OpenAPI (compatible, and identical but `x-gorbital-guards`), error codes, audit actions, permissions and roles; the golden apps' `/ops`, flags and webhook HTTP tests run against the library modules |
| 57 | Docs: *Operations API* guide updated; Methods; Shelfie chapter 5; recipe *Internal admin tool* (part 1) |

### Phase 5: sign-in extracted, unchanged

**Goal:** the generated auth module moves into the library with **no behaviour change**. Customisation is the next phase, so a regression here is easy to spot.

| # | Item |
|---|---|
| 58 | `gorbital.dev/gorbital/authhttp`: the 74 operations, use cases, SQL and migrations (same versions), `authhttp.New(opts...)` implementing `gorbital.Authenticator` |
| 59 | Configuration from `Config` exactly as today's app (providers enabled by their environment variables) |
| 60 | Commands `roles`, `grant-role`, `revoke-role`, `reset-mfa`, `rotate-auth-keys`, `auth-providers` served by `Main` |
| 61 | Contract tests: frozen OpenAPI of `/v1/auth/*`; error codes, audit actions, cookie attributes, rate-limit names; the golden app's auth HTTP tests run against the library |
| 62 | Threat model in ADR-0083 for the move: session fixation, cookie flags, CSRF with `CrossOrigin`, timing of password checks, rate-limit keys, API key hashing, impersonation off outside development |
| 63 | Docs: sign-in guides updated for where the code lives and how to configure it; Methods for `authhttp.New`; upgrade note (none required for v0.1 apps) |

### Phase 6: sign-in options, hooks and custom methods

**Goal:** the common reasons apps edited their auth module become options and hooks.

| # | Item |
|---|---|
| 64 | Options: `MinPasswordLength`, `PasswordPolicy`, `RequireMFA` (instead of ~~`MFA(TOTP, RecoveryCodes)`~~), `APIKeyMaxTTL`, `WithoutRegistration`, `Brand`, `RouteMiddleware`; ~~`Passkeys`, `Google`, `Apple`, `GitHub`, `Prefix`~~ dropped (ADR-0083) |
| 65 | Hooks: `BeforeLogin` (refuse with `authhttp.Refuse(code, detail)`), `AfterLogin` (errors logged, never block), `OnRegister` (runs in the registration transaction; error rolls back) |
| 66 | `authhttp.RegisterFields[T]`: extra registration fields validated by Huma, in OpenAPI, passed to a hook in the transaction |
| 67 | ~~`Deps.Auth`~~ `Authenticator.User(ctx, id)` and `Authenticator.SignIn(ctx, SignInRequest)`, passed to the modules that need them (ADR-0083) returning the same 200 or 202 (MFA) result as `POST /v1/auth/login` and applying bans, MFA, audit, rate limits, new-device emails and the transport |
| 68 | Threat model: hooks can't skip bans or MFA; `SignIn` requires a verified identity from the caller and is audited with the method name; refusal codes can't collide with built-in codes |
| 69 | Tests: each hook's success and failure path; transaction rollback; `SignIn` with MFA users; fuzz `RegisterFields` decoding |
| 70 | Docs: guides *Configuring sign-in*, *Sign-in hooks*, *Extra registration fields*, *Adding a sign-in method*; Methods; Shelfie chapters 6 and 7 |

### Phase 7: organisations and tenancy

| # | Item |
|---|---|
| 71 | `gorbital.dev/gorbital/orgshttp`: organisations, members, invitations, org-scoped service accounts; `orgshttp.Module(opts...)` |
| 72 | `guard.OrgMember(permission)` setting the organisation on the actor and `postgres.WithOrg` for row-level security |
| 73 | Contract tests against the full-multi golden app's frozen OpenAPI and references |
| 74 | Threat model: cross-tenant access through path IDs, RLS bypass paths, invitation token handling |
| 75 | Docs: *Organisations* and *Row-level security* guides updated; Methods; Shelfie chapter 8; recipe *Multi-tenant invoicing* |

### Phase 8: generators and CLI

**Goal:** new modules and middleware are one command away, and every route is visible from the terminal.

| # | Item | Repo |
|---|---|---|
| 76 | `orb gen module <name> <field:type>...`: the layered tree of D13 (`module.go`; `domain/`; `usecase/`, `repository/` and `delivery/` with one file per operation; `delivery/routes.go`) and its architecture test, `--org`; plan → diff → apply; `orb gen resource` kept as an alias | gorbital |
| 77 | `orb gen middleware <name>` with `--module <name>`, `--global`, `--guard`; each with a table-driven test | gorbital |
| 78 | `orb routes` (text and `--json`): method, path, handler, source file:line, guards, middleware, **public** flag | gorbital |
| 79 | `orb doctor`: module folders missing from `modules.gen.go`, stale generated files, custom stacks without Recover or Auth, pending migrations | gorbital |
| 80 | Dev Portal: Routes screen shows source, guards and public routes; generators hub has module and middleware forms using the same generator library | gorbital, gorbital-dashboards |
| 81 | Tests: golden generated files compile and pass lint inside a generated app; `--json` golden output; exit codes; non-interactive runs | gorbital |
| 82 | Docs: CLI reference; *Generating code* guide; Dev Portal pages; Shelfie chapter 9 | gorbital |

### Phase 9: upgrade, eject and new apps

**Goal:** new apps start with the new layout; existing apps can move with one command, or stay as they are.

| # | Item | Repo |
|---|---|---|
| 83 | `orb new` templates on the new layout (Full single and multi-tenant; Minimal per D17); golden apps ported; `recipes` regenerated. **Done** (2026-09-17) | gorbital |
| 84 | `orb eject <auth\|ops\|orgs\|flags\|mailevents>`: copies the library module as layered owned code, switches `main.go`, records it in `gorbital.lock`. **Done** (2026-09-17, notes below) | gorbital |
| 85 | `orb upgrade --layout v0.2` (opt-in, `--dry-run` shows the plan): keeps business modules in `internal/modules/`, rewrites `huma.Register` calls to `gorbital` verbs where mechanical, deletes generated files it finds unchanged, keeps changed ones as ejected modules, writes `UPGRADE-v0.2.md` into the app listing what it did and which changes could become hooks. **Done** (2026-09-17, notes below) | gorbital |
| 86 | Convert `portal-demo` and `copper-lantern`; their HTTP tests and `api/openapi.json` unchanged. **Done** (2026-09-17): both on their `v0.2-layout` branches, tests passing, documents compatible (`copper-lantern` needed `orb upgrade --from` to this release's v0.1 templates first) | apps |
| 87 | End-to-end tests on v0.1.0 apps: untouched, customised auth, customised middleware, extra modules. **Done** (2026-09-17): `TestLayoutMoveOnV01Apps`, with the API, `orb doctor` and a v0.1 database checked after each move | gorbital |
| 88 | Docs: *Upgrading to v0.2.0* (staying on the v0.1 layout, converting, what each report line means, before/after trees), *Ejecting a module*, *Getting started* rewritten; recipe *Upgrading a v0.1 app*. **Done** (2026-09-17) | gorbital |

Implementation notes for item 84 (2026-09-17, `framework/phase-9-eject`; [ADR-0083](adr/0083-modules-stack-migrations-and-ejection.md#phase-9-implementation-notes-orb-eject-2026-09-17), guide [Ejecting a module](guides/ejecting-a-module.md)): the copy flattens `internal/<layer>` into `internal/modules/<module>/<layer>` and keeps the package's name, so the only change to the app is import paths in every file that imports the package; tests marked `//orb:noeject` (they read this repository) aren't copied; migrations are copied into `db/migrations` and still declared by the module; `gorbital.lock` gains `ejected` with a source hash `orb doctor` compares; `orb gen modules` skips ejected modules. The built-in modules now use only public API: `gorbital.dev/gorbital/operation`, `gorbital.AuthenticateAfterInput` (enforced by the router, replacing an internal route field) and `gorbital.RetentionJob`; their internal packages sit under layers, and each module's HTTP tests carry their own test app (`gorbital/internal/ejectable` checks all of it). The generated architecture test allows another module's root package. `orgs` must be ejected before `auth`. For item 85: `planEjectFrom` (plan for one module from a library source and lock), `ejectImportRewriter` and `rewriteGoImports` (import mapping), `ejectMigrations`, and the lock's `lockEjected` entries in `cli/internal/cli/eject.go` and `lock.go`. Known gaps: going back is manual; no Dev Portal form; doc comments in the copy name library paths.

Implementation notes for items 85–88 (2026-09-17, `framework/phase-9-upgrade`; [ADR-0083](adr/0083-modules-stack-migrations-and-ejection.md#phase-9-implementation-notes-orb-upgrade---layout-v02-2026-09-17), docs [Upgrading apps](start/upgrading.md#move-to-the-v02-layout), [upgrade notes](guides/upgrade-notes.md#moving-a-v01-app-to-the-v02-layout), recipe [Upgrading a v0.1 app](examples/recipes/upgrading-a-v0.1-app.md), [CLI](guides/cli.md#orb-upgrade---layout-v02)): the move compares the app with this orb's v0.1 templates (it refuses an app plain `orb upgrade` hasn't brought up to date) and this orb's v0.2 templates; unchanged generated code is deleted, a changed built-in module is copied from the library with `orb eject`'s functions and each change placed by its surrounding lines, the app's own modules are converted in place (go/ast: `huma.Register` to the verbs or `operation.Register`, the wiring file's mappings and permissions into the `Module` value, the `Module` type renamed `Wiring`), and the app's middleware moves into `gorbital.WithStack` in `main.go` with its files. `db/migrations` is untouched, copies of the library's migrations included, so a v0.1 database has nothing to apply and the modules' own tests keep migrating from `migrations.FS`. Changes orb can't make stop the move and are listed; `--allow-manual` converts the rest and keeps those files under `_upgrade-v0.1/`. Known gaps for Phase 12: settings, flags and jobs declared in `internal/app` are reported rather than moved; an organisation-scoped module of a v0.1 multi-tenant app (which authorises through the organisations module's catalog) is left for the developer; `internal/app`'s HTTP tests aren't ported to `gorbitaltest`; the Dev Portal has no form for the move, and its job screen lists a Main app's module jobs by scanning `internal/modules` (the live list comes from `/ops/jobs`).

### Phase 10: security layers

Only layers with a clear need in the examples and the existing threat model ([ADR-0029](adr/0029-threat-model.md)). Each gets a threat model, fuzz tests for its parser and a Methods page.

| # | Item |
|---|---|
| 89 | `httpx.Timeout(d)`, built as `timeout.New(d)` in `gorbital.dev/httpx/timeout` (request context deadline, problem+json 503 on expiry, streaming routes exempt) in the default stack; per-route override `gorbital.Timeout(d)` |
| 90 | `httpx.IPFilter(allow, deny)`, built as `ipfilter.New(allow, deny)` in `gorbital.dev/httpx/ipfilter`; `/ops` limited by `OPS_ALLOWED_IPS` when set, using the client address after `TrustedProxies` |
| 91 | `guard.Webhook(verifier)` with an HMAC verifier and replay window, generalising the Resend verifier; constant-time comparison; body size limit |
| 92 | `modules/jwt`: an authenticator for external identity providers (JWKS with caching and rotation, issuer, audience, allowed algorithms, clock skew, claims to `actor`), usable as `WithAuth(jwt.New(...))` |
| 93 | Tests: expired, tampered, wrong-algorithm and wrong-audience tokens; replayed and reordered webhooks; fuzz JWT headers and signature headers; benchmarks for JWT verification with a warm JWKS cache |
| 94 | Docs: *Security layers* guide (what each protects against and its configuration); Methods; Shelfie chapter 10; recipes *Mobile backend with an external identity provider* and *Receiving payment webhooks* |

### Phase 11: Dev Portal tunnel and live sign-in tests

**Goal:** a developer exposes the local app with one click from the Dev Portal, to receive webhooks, try the API from a phone, and test Google, Apple and GitHub sign-in and passkeys on a real HTTPS domain; then checks each sign-in method works with their configuration without creating accounts. Development only.

| # | Item | Repo | Status |
|---|---|---|---|
| 95 | Tunnel: `orb dev --tunnel quick\|named` and the portal run the developer's own `cloudflared` (found on `PATH` or `ORB_CLOUDFLARED`, never downloaded; install steps per OS when missing). Quick: `cloudflared tunnel --url http://127.0.0.1:<app port> --no-autoupdate`, the `trycloudflare.com` URL parsed from its output (fuzzed), with a timeout. Named: the developer's tunnel with `CLOUDFLARE_TUNNEL_TOKEN` or its `_FILE` form, and a hostname from `ORB_TUNNEL_HOSTNAME` or saved from the portal in `.orb/portal`; reachability checked with `https://<hostname>/livez` | gorbital | Done |
| 96 | Lifecycle and security: `orb dev` owns cloudflared's process group (start, stop and restart from the portal and the CLI, stopped with `orb dev` including `Ctrl+C`, no orphans), streams its status as SSE `tunnel` events, restarts a quick tunnel when the app's port changes; refuses outside development; only the app is tunnelled; the dev console and the dev operator on `/ops/` refuse tunnelled requests (tests with public and local `Host` and Cloudflare's forwarding headers); the token never logged, returned or stored outside `.env` | gorbital | Done |
| 97 | Configuration help: the `.env` changes for the tunnel's address (`APP_PUBLIC_URL`, `WEBAUTHN_RP_ID`, `WEBAUTHN_ORIGINS`, `AUTH_DEFAULT_RETURN_TO` when it points at localhost, `APP_CORS_ORIGINS` when relevant, `APP_TRUSTED_PROXIES` for loopback) as plan → diff → apply through the env editor, which restarts the app; the Google, Apple, GitHub and Resend URLs to register, derived from the app's routes; a warning when a quick tunnel's URL is used for OAuth or passkeys | gorbital | Done |
| 98 | Tunnel screen in the Dev Portal: mode, start and stop, status, public URL with copy, reachability, cloudflared's output, install help, the `.env` proposal and the provider URLs; mock data, both themes, tests; synced into `orb` | gorbital-dashboards, gorbital | Done |
| 99 | "Test now" for Google, Apple and GitHub on the Authentication screen (linked from Routes): a test-mode round trip through the provider that checks the client ID, secret, callback URL and returned identity, and creates no account or session | gorbital, gorbital-dashboards | Done |
| 100 | "Test now" for passkeys (a registration and assertion ceremony against the configured relying party), authenticator apps (a code against a throwaway secret) and email (a message through the configured delivery), creating no accounts or sessions | gorbital, gorbital-dashboards | Done |
| 101 | Docs: ADR-0086 *Dev Portal tunnel* with its threat model; *Tunnel* page in the Dev Portal tab; `orb dev` flags in the CLI guide; *Testing on a real domain* in the sign-in provider guide; changelog and upgrade notes. Then the "Test now" ADR and pages | gorbital | Done |

Implementation notes for items 99–101 (2026-09-17, [ADR-0087](adr/0087-testing-sign-in-from-the-dev-portal.md), [Testing sign-in](dev-portal/testing-sign-in.md)): the tests are dev console extensions under `/_dev/auth/test/` (`AuthSetup.DevEndpoints`), in `authhttp/internal/delivery/signintest`. Test state lives only in the app's memory; a middleware in front of sign-in's routes answers a callback carrying a test's state and passes everything else on, so the OpenAPI document and sign-in are unchanged. Deviations: the callback redirects to the portal's own result page (which uses `BroadcastChannel` and `postMessage` to its own origin) instead of serving a page on the app's origin, because provider pages cut `window.opener` and a new response body would change the frozen contracts; the passkey ceremony page (`/_signin-test/passkey`) is on `APP_PUBLIC_URL`, so through a tunnel when that is the tunnel, inert without a console ticket; no `auth.provider.tested` audit action (tests write nothing); clock skew is a network check. Known gaps: one process holds test state; a real authenticator keeps the throwaway passkey on browsers without `signalUnknownCredential`; no screenshots of the tab.

Considered and rejected by the maintainer (2026-09-17): switching sign-in providers on and off from the portal. A toggle that turns off the only method an operator uses locks them out, and the configuration already decides which methods exist; "Test now" checks a configured method instead.

### Phase 12: review and release

| # | Item | Repo | Status |
|---|---|---|---|
| 102 | Shelfie and every recipe complete in `examples/apps/`, built and tested in CI | gorbital | |
| 103 | Docs review: every guide read against the code; every exported identifier in Methods with an example; every v0.1 page that changed has a v0.2 counterpart; no broken links in any version | gorbital, gorbital-web |
| 104 | Benchmarks compared with the Phase 0 baselines; results in `docs/benchmarks.md` | gorbital | |
| 105 | Internal security review of the v0.2 diff (auth extraction, guards, hooks, JWT, webhooks); report in `docs/security/` | gorbital | Done (2026-09-17): [report](security/2026-09-v0.2-internal-review.md). 17 findings — 1 High, 6 Medium, 6 Low, 4 Info; ten fixed with a regression test, seven left to the maintainer with a written reason. High: `OPS_ALLOWED_IPS` reached only `opshttp`'s own routes, so sign-in's `/ops/auth/users` and `/ops/service-accounts` — ban, grant a role, revoke sessions, impersonate, mint an API key — answered from any address. Medium: `gorbital.Customize` could replace a route's sign-in check and guards while the document still declared it protected; `modules/jwt` lost the JWKS redirect allowlist to an app's own HTTP client, and passed a provider claim's `ops.*` permissions into the actor; `httpx/timeout`'s `Unwrap` raced the 503 on the header map; `ParseTrustedProxies` ignored IPv4-mapped entries and re-based short mapped ranges; `orb upgrade --layout v0.2` regraded org-scoped permissions past the twentieth resource. Also: ten "tested" claims in the phase threat models (ADR-0082, ADR-0083, ADR-0085, ADR-0086, ADR-0087) are not backed by a test that exercises the hostile case, and seven more only weakly — one of them, `Customize`, was false. ADR-0029, the changelog and the upgrade notes are updated | `framework/phase-12-12a` |
| 106 | Website (`apps/www`): home and product pages show the new `main.go` and module | gorbital-web | |
| 107 | Final changelog, upgrade notes and this roadmap's statuses | gorbital | |
| 108 | Merge `v0.2` into `main`; bump `recipes.LibraryVersion`; release with `scripts/release.sh`, at most 3 tags per push so the release workflows run | gorbital | |
| 109 | Docs site: latest becomes v0.2, v0.1 stays at `/v0.1/`, `/next/` follows the next branch | gorbital-web | |
| 110 | Clean-machine check: `go install gorbital.dev/cli/cmd/orb@latest`, `orb new`, Shelfie from the docs, a v0.1.0 app building against v0.2.0, `orb upgrade --layout v0.2` on it | — | |
| 111 | Announcement in `marketing/launch-posts` | marketing | |

Implementation notes for items 102 and 103 (2026-09-17, `framework/phase-12-12b`): the Examples tab is Shelfie's twelve chapters and five recipes, backed by `examples/apps/{shelfie,admin-tool,invoicing,payments,mobile-backend}`, which CI's *example apps* job builds and tests. Chapters 2, 3, 10 and 11 and the *Receiving payment webhooks* and *Mobile backend with an external identity provider* recipes were written here, with the code they explain; the *Internal admin tool* gained the part 2 it promised, which meant giving that app sign-in at last. `internal/tools/docscheck` checks every Markdown link, `#anchor` and `<!-- include -->` marker and `docs/docs.json`, in CI. Decisions: chapter 1 generates the module with `orb gen module` and reads Shelfie's own files rather than pretending the finished module is a chapter-1 subset (it isn't — `routes.go` calls chapters 2 and 3's code); chapter 0 starts from `orb new`, which has written the v0.2 layout since Phase 9; the reference pages gained a scan of library modules no golden app links, rather than adding `modules/jwt` to a golden app. Known gaps: `docs/docs.json` still says `"version": "v0.1.0"` and announces v0.1.0, which item 109 changes at the release; `cli/internal/recipes/recipes.go`'s `LibraryVersion` is still `v0.1.0`, so `orb new` writes that requirement into a v0.2 app (item 108); chapter 0's counts of a new app's files and operations are exact and will drift as `/ops` grows; `docs/start/quickstart.md` prints real-looking passwords, two-factor keys and recovery codes where Shelfie's chapter 0 now uses placeholders.

---

## Not in v0.2

Considered during planning and deliberately left out. Each needs an ADR with a concrete use case before it is scheduled.

| Idea | Why not now | What to do instead today |
|---|---|---|
| Event bus or outbox module | Transactional side effects already work: `jobs.Client.InsertTx` enqueues in the same transaction as the write | Document the pattern in the *Receiving payment webhooks* recipe |
| Redis backends, cache module | PostgreSQL is the only required datastore; `ratelimitpg` and `idempotency` already share state across instances | An in-process cache in the app where measured |
| Response compression, ETag, load shedding | Usually done by the reverse proxy or CDN; adds buffering and edge cases to every response | Configure at the proxy; revisit with measurements |
| CAPTCHA module | Needs a provider choice and UI integration; the rate limits and bans already slow credential stuffing | `BeforeLogin` hook calling the provider |
| Outbound HTTP client with retries and circuit breaker | Useful, but a design of its own (retry budgets, idempotency of outbound calls) | `net/http` with timeouts and OpenTelemetry's `otelhttp` |
| Outgoing webhooks, upload handler | Product features with their own storage and delivery design | Jobs plus `modules/storage` signed URLs |
| WebSocket | No example needs it; SSE covers server-to-client streams | The existing streams in `observability` as a reference |
| Route annotations (Nest-style `//gorbital:route`) | See D7. Can be reconsidered after v0.2 with real usage of the explicit API, as a generator on top of it | Explicit `gorbital.Post(...)` in `Module.Routes`; `orb gen module` writes the first routes |
| File-based routing | See D6 | Explicit routes |

## Risks

| Risk | Mitigation |
|---|---|
| The public surface grows by hundreds of identifiers and 100+ operations to keep compatible | `apicheck`; frozen v0.1.0 contract fixtures; additive-only rule (D2) |
| Moving sign-in changes a security property by accident | Extraction without behaviour change first (Phase 5), contract tests, threat models, internal review before release |
| Hooks don't cover what real apps changed | Phase 9's upgrade report lists every customisation found; each becomes a hook or a documented reason to eject |
| `gorbital.New` becomes a god constructor | It only orders calls to existing constructors; every step stays callable on its own; documented construction order; D4 keeps it testable |
| The facade pulls heavy dependencies into small apps | Drivers passed by the app (D3), dependency and binary budgets in CI, D17 for Minimal |
| Two doc versions double the writing | Methods generated; examples included from compiled code; v0.1 accepts only fixes |

## Changes from the first draft

The first draft (commit `38c0c33`) was reviewed on 2026-09-17 against the codebase and the project's principles. What changed and why:

| First draft | Now | Why |
|---|---|---|
| v0.2 as a breaking release that converts every app | **Additive**; v0.1 apps build against v0.2; conversion opt-in (D2) | Nothing forces a break: extraction adds packages; users upgrade when ready |
| Auth, ops and drivers on implicitly (`WithoutAuth` to remove) | **One explicit line per built-in** in `main.go` (D3) | What the app contains is visible; small apps don't compile what they don't use |
| `gorbital.Run(cfg)` only | `LoadConfig` → `New` → `Run`, plus `Main` (D4) | Testable, embeddable, errors reported instead of exiting |
| `guard.Auth()` added to each route | **Deny by default**, `guard.Public()` to opt out (D9) | A forgotten guard can't expose a route |
| `Before`/`After`/`Replace`/`Without`/`IKnowWhatImDoing` | `WithStack(func(Stack) []httpx.Middleware)` (D11) | Plain Go instead of a positional mini-language |
| Per-module migration tables | One goose history, same versions (D14) | Cross-module foreign keys, and existing databases see no change |
| Migrations as part of `Run` | Only from `migrate` | [ADR-0017](adr/0017-application-lifecycle.md): never implicitly at startup |
| Annotations as the default, with `use` expressions in comments | Explicit API only; annotations not in v0.2 (D6, D7) | Code in comments isn't type-checked; the explicit API solves most of the boilerplate |
| Auth moved and customised in one phase | Extraction unchanged (Phase 5), then options and hooks (Phase 6) | Regressions are visible when behaviour isn't changing at the same time |
| Test kit in the last phases | Phase 3 | Every later phase and example uses it |
| Redis, cache, compression, ETag, load shedding, CAPTCHA, HTTP client, outgoing webhooks, uploads, event bus, WebSocket | [Not in v0.2](#not-in-v02) | No committed use case; some contradict "PostgreSQL only"; `InsertTx` already covers transactional side effects |
| No quality gates beyond existing CI | [Engineering standards](#engineering-standards): fuzzing, benchmark budgets, v0.1-app compatibility job, frozen contracts, threat models | A framework's guarantees need tests that enforce them |
