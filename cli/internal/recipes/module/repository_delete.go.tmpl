package repository

import (
	"context"

	"⟦.Module⟧/internal/modules/⟦.Package⟧/domain"
)

⟦- if .Filtered⟧
const delete⟦.Ident⟧SQL = `DELETE FROM ⟦.Table⟧ WHERE id = $1 AND ⟦.ScopeColumn⟧ = $2`

// Delete⟦.Ident⟧ removes one of ⟦.ScopeVar⟧'s ⟦.PluralHuman⟧, or returns
// Err⟦.Ident⟧NotFound.
func (s *Store) Delete⟦.Ident⟧(ctx context.Context, ⟦.ScopeVar⟧, id string) error {
	tag, err := s.db.Exec(ctx, delete⟦.Ident⟧SQL, id, ⟦.ScopeVar⟧)
⟦- else⟧
const delete⟦.Ident⟧SQL = `DELETE FROM ⟦.Table⟧ WHERE id = $1`

// Delete⟦.Ident⟧ removes the ⟦.Human⟧ with this ID, or returns
// Err⟦.Ident⟧NotFound.
func (s *Store) Delete⟦.Ident⟧(ctx context.Context, id string) error {
	tag, err := s.db.Exec(ctx, delete⟦.Ident⟧SQL, id)
⟦- end⟧
	if err != nil {
		return err
	}
	if tag.RowsAffected() == 0 {
		return domain.Err⟦.Ident⟧NotFound
	}
	return nil
}
