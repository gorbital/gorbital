# gorbital project file. orb dev reads features: postgres starts the
# database from compose.yaml and migrates it before the app starts.
apiVersion: gorbital.dev/v1
name: shelfie
module: example.com/shelfie
preset: full
tenancy: single
features: [postgres]
