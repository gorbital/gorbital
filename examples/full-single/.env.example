# Copy to .env for local development. Never commit .env.
#
# Only secrets and infrastructure live here. Values operators change at
# runtime (like example.ping_message or the email sender) are runtime
# settings, edited through /ops/settings, and job schedules are edited through
# /ops/jobs.

# development or production. Production enables JSON logs and HSTS.
APP_ENV=development

# Address the API listens on.
APP_ADDR=127.0.0.1:8080

# debug, info, warn or error.
APP_LOG_LEVEL=info

# Serve interactive docs at /docs: true or false.
APP_DOCS_ENABLED=true

# Comma-separated browser origins allowed to call the API (CORS), for example
# https://app.example.com. Empty disables CORS.
APP_CORS_ORIGINS=

# Largest accepted request body in bytes.
APP_MAX_BODY_BYTES=1048576

# OpenTelemetry export. Set an OTLP endpoint to send traces and metrics,
# for example http://localhost:4318. Empty keeps telemetry local.
OTEL_EXPORTER_OTLP_ENDPOINT=

# PostgreSQL, from compose.yaml in development. In production use a secret
# (DATABASE_URL_FILE is also supported).
DATABASE_URL=postgres://acme:acme@127.0.0.1:5432/acme?sslmode=disable

# Host port for the development database in compose.yaml.
POSTGRES_PORT=5432

# Maximum database connections per instance.
APP_DB_MAX_CONNS=10

# Jobs worked at the same time per instance.
APP_JOB_WORKERS=10

# Email delivery: mailpit sends every email to the local inbox from
# compose.yaml (read it at http://127.0.0.1:8025); provider sends real email.
# Empty means mailpit in development and provider in production.
MAIL_DELIVERY=

# Mailpit's SMTP address, and its host ports in compose.yaml.
MAILPIT_SMTP_ADDR=127.0.0.1:1025
MAILPIT_SMTP_PORT=1025
MAILPIT_WEB_PORT=8025

# aps:begin mail (managed by `aps add mail`: the email provider's secrets)
# Resend API key, from https://resend.com/api-keys. Needed when email goes
# through Resend (always in production). The sender name, address and
# reply-to are runtime settings: PUT /ops/settings/mail.from_email.
RESEND_API_KEY=
# aps:end mail
